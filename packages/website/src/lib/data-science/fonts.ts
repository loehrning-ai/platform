/**
 * The data-science course deliberately uses repository-local and system font
 * stacks. This keeps clean builds offline and avoids a hidden Google Fonts
 * download during `next build`.
 */
export const DS_FONT_VARIABLES = "ds-fonts-local";
