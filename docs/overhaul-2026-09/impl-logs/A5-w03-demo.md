# A5-w03-demo change log

## Files changed
- `scripts/course03/demo/demo.html`: rebuilt from scratch (98 KB, was 135 KB). Six beats named after the deck route (Wrong answer, Why it failed, The fix, Ask again, Honest limits, Your turn). Every number is in the HTML on load. The prediction vote (fieldset, square radios, role=status) sits above the check. Components drawn in HTML/CSS: q-card (the only left bar), ev-run card with hatch swatch, HTML truth chart (hatch = AI run on export tables, slate = database check, 3px zero line, sr-only table), cause card, sum card (the €32,380 is the one Mennige mark in beat 2), four blanks, the four filled answers plus a "Where each part lives" ledger, a run/database-check table with `=` pairing and "Matches" chips (green only with icon and word), two dashed known-gap chips, course rules plus the forced read, upright stamps. Ending MRR carries the page. Net new MRR and logo churn are two compact "same pattern" rows. "Under the hood" is collapsed and built by JS from the embedded data: table explorer for both logins, SQL of all six runs, the forced read, and the 22 checks. Motion happens only at three moments: a one-off count-up of the wrong values, "Replay the check" (1.2s; hidden under reduced motion), and the route station fill. Nothing starts at opacity 0. There are no inline handlers. The `wf-strip` markup and scripts are unchanged, and its CSS now matches `lib/workshop-frame.css` of W01/W02 (text tabs with square stations).
- `scripts/course03/demo/sync-values.mjs` (new): the one place that turns `demo-data.json` into the page's static text (`data-v="key"`) and bar heights (`data-bar`). It re-embeds the JSON block if it differs. Use `--check` to detect drift. It computes currency with a true minus (−€19,960), picked relations, the net-new column of the approved view, the sum, G02/G03 details, definition fields, and provenance.
- `scripts/course03/demo/capture.py`: now also stores `recorded` (capturedAtUtc plus summary, from `model-capture-data.js`) and `rules` (C01/R01/R02 from `replay-data.js`, with the deck's labels), and calls `sync-values.mjs` after it re-embeds. The embed regex contract is unchanged.
- `scripts/course03/demo/demo-data.json`: `recorded` and `rules` were added the same way `capture.py` would add them. No other changes.
- `scripts/course03/card-preview.mjs` (new): serves the published folder on loopback, renders `slides.html#cover/0` at 1920x1080 with reduced motion, and uses sharp to write `card-preview.webp` at 1024x576. `refresh-published` then writes the ASSET_MANIFEST row.
- `scripts/course03/guide.html`: CSS de-brutalised. Offset shadows removed. Mono uppercase eyebrows, toc and toggles are now sentence-case 600 labels. h1 tracking goes from -.045 to -.015em. A Kopflinie now sits above each h2. 2px boxes became 1px. The example is a q-card with an ink bar, and the Mennige left bar is gone. The note is a Beton band. The gates are hairline rows. The nums use € and a true minus, in sans instead of mono and with no red/green. Copy changes: "Words without the fog" is now "Glossary". "No coding" is now "You work in a browser and in Claude". The aphorism "Instructions guide, grants enforce" (x2) was rewritten as plain sentences. Kept: "Reveal the explanation" and "Open the interactive course" (e2e).
- `scripts/course03/builder/page/builder.html`: CSS pass. Offset shadows and the door hover lift are removed. All uppercase tracked labels are now sentence-case. The mono orange eyebrow now uses the label style. h1 tracking goes to -.015em. Mennige/ink inset side stripes became outlines or tone. 3px and 2px ink boxes became 1px, and 3px rules became 2px. En dashes "G01–G05" were changed to "G01 to G05". Markup and JS are untouched.
- Published by `node scripts/course03/refresh-published.mjs`: `public/workshops/datenbereitschaft-fuer-ki/{demo.html,guide.html,builder.html,card-preview.webp,bundle-manifest.json}` plus the card-preview row in `ASSET_MANIFEST.json`.
- `packages/website/tests/e2e/workshop-demo.spec.ts` (new): at 1440 and 390 it checks that the final numbers show on load (−€19,960, €334,675, €32,380, "Right answer to a different question", "Values match"), that the vote comes before the check in reading order, that voting updates the status and the echo, that there is no horizontal overflow and no hidden sections, that Under the hood renders, and that there are no console errors. It also runs axe (wcag2a/aa, 21aa, 22aa) and checks the reduced-motion replay button.

## Checks run
- `node scripts/course03/refresh-published.mjs --check`: up to date. `overrides.mjs check`: match. `sync-values.mjs --check`: match.
- `node --test scripts/__tests__/course03-publication.test.mjs`: 7/7.
- `bunx vitest run src/lib/workshops-data-readiness.test.ts`: 7/7. `workshops-static-links`, `workshops.test`, `crawl/contract`: all pass.
- e2e spec: 4/4 against the running dev server. The repo config wants the headless shell, so I ran it with a temporary config pointing at /opt/pw-browsers/chromium. For the integrator: `E2E_REUSE_EXISTING_SERVER=1 bunx playwright test tests/e2e/workshop-demo.spec.ts --project=chromium`.
- `bun run content:lint`: 0 errors, no warnings on demo or guide. `scan:public`: the only blocking findings are W01 `Typing-Medium/SemiBold.woff2` missing from ASSET_MANIFEST (not mine).
- eslint on the spec: clean.

## Measurements (screenshots in scratchpad/impl/A5/)
- before: desktop 6,166px (6.9 screens), phone 10,609px (12.6), with a ~2,900px blank band and no answer on the first screen.
- after: desktop 5,203px (5.8 screens), phone 7,843px (9.3), with every number visible at load, 0 console errors and 0 horizontal overflow at 390.
- The phone is still above the audit's ~6-screen goal. Getting there would mean cutting content (for example collapsing the beat 3 ledger or the cause card); I kept the deck's full argument.

## Decisions and notes for the integrator
- Fact check: the audit-copy note "one database with two logins" is wrong. `00_build_all.sql` creates two databases (`saas_bad` for the export tables, `saas_ready` for the approved views), each with its own reader login (check B-X01: the ready login has no CONNECT on the bad database). The page therefore says neither "two databases" nor "one database". It says "a login that can read nothing else". Please don't reintroduce "one database with two logins" in `workshops-data-readiness.ts` copy.
- Material label "Interaktive Demo · 10 Min." / "Interactive demo · 10 min" stays valid (route times sum to 10 min). There were no changes to `workshops-data-readiness.ts`/test.
- `scripts/course03/builder/source/build.py` + `page.css` still drift from the published builder page (pre-existing: no strip, old title, and now also the old brutalist CSS). Do not rerun build.py. Either retire it or port `builder/page/builder.html`'s CSS back.
- W03 pages inline the strip CSS rather than linking `lib/workshop-frame.css`. Linking it would need `isRepositoryAuthored()` and the `authored` map extended and would re-hash the bundle on every frame edit. The inline block matches the frame's strip rules.
- The card preview now shows cover step 0 (title, question card, globe) without the arc chips of the old export. Re-render with `node scripts/course03/card-preview.mjs [--chromium path]` and then `refresh-published`.
- The embedded JSON is now serialised by JSON.stringify (334675 instead of 334675.0). It parses to the same object; `capture.py` will rewrite it in Python style on the next recapture, which is fine.
