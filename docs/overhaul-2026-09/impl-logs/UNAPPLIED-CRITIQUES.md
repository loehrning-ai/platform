# Design and accuracy critiques

Polish passes that failed on the usage limit did not apply these. Check each against the current file before fixing.

### critique:B1-workshops-hub

Verdict: Close, but it needs one more pass before it ships. The hub now reads as calm and editorial: a graphit cover band with a static globe, a Kopflinie and h2 per section, a solid 5-station Route, and hairline-separated rows with cover left and text right. There is one paper-on-graphit primary button, sentence-case 600 labels at +0.02em and 700 headlines at -0.015em. I found no pastel, no offset shadows, no rotation and no orange left rules inside <main>. Checks I ran myself: axe (wcag2a/aa, 21aa, 22aa) finds 0 violations on /workshops and /en/workshops at 390, 1024 and 1440. There is no horizontal overflow, and the type floor (13px) holds. Four things stop it looking finished: (1) the three cover images do not match, W01 is a light paper page with an orange "decision", and W02 has an orange "Decisions"; (2) at 1024 the globe's Mennige Germany shape sits under the lead text and globe lines cross the H1; (3) the old risograph nav (acid DE toggle, cobalt LOGIN, pill) and footer (orange mono uppercase headings, decorative circles) still frame the page in most captures; (4) small copy and data problems: "Für Datenteams" listed as a material, "kostenlos" said three times, a clumsy W02 requirement, a U+2212 minus that renders like an em dash, and "du öffnest sie mit P". Screenshots are in /tmp/claude-0/-home-user-platform/614e303c-f8f0-55ca-be18-13442a9af90b/scratchpad/impl/B1-workshops-hub-critique/: de/en-{1440,1024,390}-full.png, de-1024-fold.png, c-row03.png, c-team.png, focus.png and m-*.png. Environment caveats: in some fresh contexts the dev server painted the fallback font (DejaVu-like) before Loehrning Sans loaded. This showed up in m-fold.png, de-1024-fold.png and focus.png. It is probably a dev-server font race, but check font-display/preload in a production build. The Next dev "N" badge is not a page issue. Also, the nav was changing between my captures because another agent is editing it.

- [high] Workshop row covers: public/workshops/ki-prognosen-einschaetzen/card-preview.webp (W01), public/workshops/geschaeftsberichte-mit-ki-lesen/card-preview.webp (W02), rendered by src/app/workshops/workshops-content.tsx WorkshopRow <figure>: The three covers do not match. W01 is a light paper hub screenshot with small cards, a sparkline and a two-tone orange "decision". W02 is a dark app-like page with a two-tone orange "Decisions" and mono-uppercase micro text. Only W03 is a real Werkzeichnung deck cover. W01 is the loudest leftover of the old look on the page and breaks the "all cards uniform" rule in 6.7. Removal list items 7 and 14 apply to these covers. Fix: Regenerate every card-preview.webp from slides.html#cover/0 at 1920x1080, downscaled to 960x540 webp (design-direction 10.4). Until W01 and W02 have a restyled deck cover, WorkshopRow should render the CSS mini-cover fallback from 6.7 instead of the image. That fallback is `div.aspect-video.bg-dark-bg` with the globe on the right and the title in `text-[1.25rem] font-bold text-dark-fg`. Gate it with a registry flag such as `workshop.coverIsDeck`, or with an allow-list in workshops-content.tsx.
- [high] src/app/workshops/workshops-content.tsx CoverBand children (h1 `max-w-[18ch]`, lead `max-w-[56ch]`); src/components/werk CoverBand globe layer: At 1024x768 the Mennige Germany shape sits under the end of the lead's first and second lines ("...Firma. Du", "...wie das"). Globe lines run through the H1 ("einem", "Vorlage"). Text over drawing is not calm, and the band's one Mennige mark competes with the copy. At 1440 the layout is clean. Fix: In workshops-content.tsx, change the h1 to `max-w-[14ch] lg:max-w-[18ch]` and the lead to `max-w-[min(56ch,58%)] md:max-w-[46ch] xl:max-w-[56ch]`. In CoverBand (owner of werk), start the globe at lg instead of md (`hidden lg:block`). Alternatively, widen its left mask to `[mask-image:linear-gradient(to_right,transparent,black_45%)]` for md..lg, so nothing drawn sits under the text column.
- [high] Site chrome on this page: src/components/nav.tsx and src/components/footer.tsx (not owned by B1): In the 1440 and 390 captures the page is framed by the old look. The nav is a floating shadowed pill with an acid DE toggle and a cobalt LOGIN button. The footer has orange mono-uppercase headings ("FREIE LERNPLATTFORM", "KURSE", "PRAXIS"), mono uppercase "DATENSTAND", and decorative outline circles. In one later capture the nav was already flat, so it is changing mid-edit. The footer had not changed. This is the first and last thing a visitor sees. Fix: Integrator: confirm the nav owner lands 8.15 (flat paper nav with a 1px hairline, a square ink-underline DE/EN switch, Login as a secondary ink button). Confirm the footer owner lands 8.4, 8.10 and 8.11: footer headings become `text-label font-semibold text-dark-fg` in sentence case (no `font-ui-mono uppercase tracking-*`, no `text-brand-orange`), "Datenstand" becomes caption text with mono kept only for the date, and the decorative circle elements are deleted.
- [medium] src/app/workshops/workshops-content.tsx WorkshopRow `roles` (built from WORKSHOP_PAGE_COPY[locale].detail.roleLabels); workshop-copy.ts detail.roleLabels.builder = "Für Datenteams" / "For data teams": The W03 Material line reads "Deck · Moderation · Demo · Kit · Lernbegleiter · Labor · Für Datenteams". "Für Datenteams" is an audience, not a material. It is also the item that forces the orphan wrap "Für / Datenteams" at 1440 and "· Für Datenteams" at 1024. Seven items also makes W03 look much heavier than W02 ("Deck · Kit"). Fix: Add a hub-specific noun map in the catalog copy, for example `catalog.materialNouns: { builder: "Bauanleitung" }` (EN "Build guide"), and use it before falling back to detail.roleLabels. Cap the list at 4 items plus "+N", or order it deck, demo, kit, companion and fold the rest. Add `[&>span]:whitespace-nowrap` around each item so the separator stays with its word: wrap each role in `<span className="whitespace-nowrap">` and put the " · " joiner inside the span.
- [medium] src/lib/workshops-data-readiness.ts summary DE l.20 ("MRR-Endbestand von −19.960 €"), shown in the W03 row: Loehrning Sans draws U+2212 as a long bar. On the page it reads "von —19.960 €", which looks like an em dash (banned by the voice rules) and makes the number hard to read. The same glyph is in the W03 detail copy. Fix: On the hub, show the number as `<span className="tabular-nums whitespace-nowrap">-19.960 €</span>` with ASCII hyphen-minus, or rephrase to "einen negativen MRR-Endbestand von minus 19.960 €". Ask the registry owner to apply the same change to summary and description in DE and EN. The EN test already expects "-19,960" with a hyphen, so the EN copy is consistent with that.
- [medium] src/app/workshops/workshop-copy.ts catalog.hubKicker, catalog.listCaption, catalog.hubAccess (DE + EN): "kostenlos" appears three times above and at the list: the kicker "Workshops · 3 Fälle · kostenlos", the access line "Alle Materialien kostenlos, ohne Anmeldung" and the list caption "3 · kostenlos". The caption "3 · kostenlos" is also a bare count-first fragment with no noun. Fix: Kicker: `Workshops · ${count} Fälle` (EN `Workshops · ${count} cases`). listCaption: "Neueste zuerst" (EN "Newest first"), which is factual and explains the order. Keep "kostenlos, ohne Anmeldung" only in hubAccess.
- [medium] src/app/workshops/workshops-content.tsx `limitingNeed()`; registry needs[0] for W02 in src/lib/workshops*.ts: W02's "Du brauchst" reads "Die Claude-Desktop-App mit Claude Code, in einem Plan, der Claude Code enthält". That is a doubled, clumsy phrase and the longest line in the row. The EN version has the same problem. Fix: Add an optional `workshop.requirementShort` (DE "Claude-Desktop-App und ein Claude-Plan mit Claude Code", EN "Claude desktop app and a Claude plan that includes Claude Code"). Have limitingNeed return it before needs[0]. If the registry cannot change now, put a slug-keyed override map in catalog copy.
- [medium] src/app/workshops/workshops-content.tsx third <section aria-labelledby="workshop-teams-heading"> + workshop-copy.ts catalog.teamsBody: The "Mit deinem Team" section has weak hierarchy. It gets a full 2px Kopflinie and a 36px h2, the same weight as the list, for two lines of text. The text is insider jargon: "Workshops mit Deck haben eine Moderationsansicht...; du öffnest sie mit P". A reader does not know which workshops have a deck or what P means, and the semicolon construction is stiff. The boundary callout below then floats at 13px with 40px of air. Fix: Downgrade it. Drop the SectionHead and render `<div className="mt-10 grid gap-2 border-t border-hairline pt-6 max-w-[64ch]"><h2 className="text-[1.25rem] font-bold">Mit deinem Team</h2><p className="text-body text-muted-foreground">…</p></div>` directly after the list inside the list section, then the Callout at mt-6. Copy DE: "Workshop 03 hat eine Moderationsansicht mit Notizen und Abstimmungsfragen. Öffne das Deck und drück P. Was du für eine Gruppe brauchst, steht auf der Workshop-Seite." EN: "Workshop 03 has a presenter view with notes and voting questions. Open the deck and press P. What you need for a group is on the workshop page." Derive the workshop list from materials with role "facilitation" so W04 is included automatically.
- [medium] src/app/workshops/workshops-content.tsx `orderWorkshopsForHub` + `first` used for ButtonLink (copy.hubStart): The cover button follows newest-first order. When W04 lands it silently becomes "Mit Workshop 04 beginnen". The workshop standard names 03 as the recommended start. The button is a recommendation, and newest is a different thing from recommended. Fix: Add `const RECOMMENDED_START = "03"` in workshops-content.tsx and resolve `first = workshops.find(w => w.number === RECOMMENDED_START) ?? ordered[0]`. Keep newest first for the list. Add a unit case in workshops-content.test.tsx with the stand-in W04 asserting the button still targets 03.
- [low] src/app/workshops/workshops-content.tsx WorkshopRow <article> (className l.217): The whole row is clickable through the stretched ::after, but keyboard focus draws the 3px Mennige ring only around the "Workshop ansehen" link (focus.png). Section 6.7 and section 9 ask for the ring on the whole card, so keyboard users see the same hit area as mouse users. Fix: Append `has-[a:focus-visible]:outline has-[a:focus-visible]:outline-[3px] has-[a:focus-visible]:outline-offset-4 has-[a:focus-visible]:outline-brand-orange` to the article. On the link, add `focus-visible:outline-none` only if the article ring is present; keep a fallback via `@supports not selector(:has(*))`.
- [low] src/app/workshops/workshop-copy.ts EN catalog (hubStart, leaveWith): EN is inconsistent with the rest of the page. The button says "Start with workshop 03" (lowercase w), while the kickers say "Workshop 03". The hub label is "What you leave with", while the detail page uses "You leave with". The DE labels are consistent. Fix: Set `hubStart: (n) => `Start with Workshop ${n}`` and `leaveWith: "You leave with"` in the EN catalog block.
- [low] W03 row kicker ("Live 90 Min.") vs W03 cover image caption ("75 minutes") vs Route caption ("75 bis 90 Minuten"): Three durations sit next to each other for W03: 90 live in the kicker, 75 on the cover image and 75 to 90 in the route caption. A careful reader will wonder which one is right. minutesLive is deck 75 plus 15 by design (see the workshops-data-readiness test), but the page never says so. Fix: Either render "Live 90 Min. (75 Min. Deck + Fragen)" in the kicker when `minutesLive !== deckMinutes`, or change routeCaption to "60 bis 90 Minuten" (the real range across the rows: self-study 60 to 90, live 90), and regenerate the W03 cover without the minutes line. Pick one and state it the same way everywhere.
- [low] src/app/workshops/workshops-content.tsx Route in the "So läuft jeder Workshop" section; werk Route: At 1440 and 1024 the five stations stop at about 80% of the container. The last square sits at x≈980 of 1196, while the Kopflinie and the row hairlines above and below run the full width, so the Route looks cut off. The station descriptions are 13px Schiefer; paired with 14px titles in 20% columns, they wrap into short ragged lines ("festgelegt / hat"). Fix: In Route description mode, use `grid grid-cols-5` with the connecting line drawn to the last station, and right-align the last station, or let the last column be `auto` so the line spans the full container. Alternatively, cap the Route at `max-w-[60rem]` so it looks deliberately narrower. Raise the descriptions from `text-caption` to `text-[0.875rem] leading-snug` and add `text-pretty` to avoid one-word last lines.
- [low] src/app/workshops/workshop-copy.ts DE catalog.hubHeading (390 viewport): At 390 the H1 "Workshops mit einem Fall und einer Vorlage für deine Arbeit" runs 4 to 5 lines at 44px and pushes the button to about y=590. The headline also says "Workshops" twice with the kicker right above it. It is plain, but it is long for a hub title. Fix: Shorten to "Ein Fall, eine Frage, eine Vorlage für deine Arbeit" (avoid; triplet) → better: "Workshops mit Fall und Vorlage" (DE) / "Workshops with a case and a template" (EN). Move the "für deine Arbeit" idea into the lead, which already says it.
- [low] CoverBand bottom spacing in src/app/workshops/workshops-content.tsx (nav `mt-10 ... sm:mt-12`, CoverBand padding): Below the index row the band has about 100 to 110px of empty graphit at 1440 and 1024, more than above the kicker, so the band feels bottom-heavy. On phones the rail cuts "03 Datenb…" with no end padding, and the last item cannot scroll fully clear of the edge. Fix: Reduce the band's bottom padding to match the top: CoverBand `pb-12 lg:pb-16`, or have the index nav end the band with `pb-0` and the band `lg:pb-14`. On the rail `ol`, add `pr-4 scroll-px-4` (phone) so the last item snaps fully into view.

### critique:B2-workshop-detail

Verdict: Nothing needs a rebuild; this is a solid pass with a list of polish fixes. I checked 15 page and viewport combinations: the three workshops in DE at 1440, 390 and 1024, and in EN at 1440 and 390. axe (WCAG 2.2 AA) found no violations, including the lab after a wrong answer. There is no horizontal overflow and there is one h1 per page. A computed-style scan of <main> found none of the old look: no uppercase text, no mono text, no box-shadows, no left borders of 3px or more, no border-radius, no text under 12px, no Mennige-coloured text and no em/en dashes. The page reads calm and editorial and nothing brutalist is left. What's left is polish:

- Hierarchy is flat below the fold: nine h2s all at 36px, against a 52px h1.
- On phones every material row is about 280px tall because the chip and the link sit on separate lines.
- The Route has no link to the lab below it.
- Several captions wrap badly, including the orphaned "Anmeldung" on the cover caption at 1440 and 1024.
- One lab feedback string is wrong: a wrong decision is labelled "nicht die stärkste", which is the evidence wording.
- The copy repeats the same facts ("erfunden", free with no sign-up, "Material auf Englisch", recording dates) three to five times per page, and the lab titles use the count-first fragment style the voice rules ban.

Screenshots and scripts are in /tmp/claude-0/-home-user-platform/614e303c-f8f0-55ca-be18-13442a9af90b/scratchpad/impl/B2-workshop-detail-critique/: the w0X-*-full.png files, the -sN.png slices, and lab-wrong/lab-right-*.png. When I ran the screenshot script I briefly copied it into the repo as packages/website/.crit-shot.mjs, before the failing run. The run did not use that copy and I deleted it straight away, so no repo file was changed.

- [high] src/app/workshops/[slug]/workshop-decision-lab.tsx, COPY.de.yourWrongPick (line ~56) and the EN equivalent; the same label is shown on the decision fieldset: After a wrong submit, the wrong option in the first fieldset (Deine erste Entscheidung) is labelled "Deine Wahl · nicht die stärkste". "Stärkste" is the evidence wording, so the feedback on the decision question is wrong. I saw this in lab-wrong-1440.png. Fix: Split the key by fieldset: yourWrongDecision "Deine Wahl · nicht richtig" / "Your pick · not correct", and yourWrongEvidence "Deine Wahl · nicht der stärkste Beleg" / "Your pick · not the strongest evidence". Choose the key by the fieldset the option belongs to.
- [medium] src/app/workshops/[slug]/workshop-detail-content.tsx MaterialRow (lines ~175-190): the chip <div className="col-start-2 sm:col-start-auto"> and the WorkshopMaterialLink with col-start-2: On a 390px phone the meta chip and the Öffnen link take separate grid rows. Each material row is about 280px tall, and W03's seven rows make roughly 1,900px of scrolling. Blueprint 7.2 asks for [meta chip, action] on one line. Fix: Wrap both in <div className="col-start-2 flex items-center gap-4 sm:contents">. Keep the chip wrapper as sm:col-start-auto and on the link drop col-start-2 and self-start (keep sm:self-center). Each row loses about 56px on phones and the desktop grid is unchanged.
- [medium] workshop-detail-content.tsx MaterialRow <li>: sm:grid-cols-[2rem_minmax(0,1fr)_auto_7.5rem] sm:items-center: On desktop the pictogram is vertically centred in a 3-4 line row, so it floats about 50px below the title it labels. Because the chip column is 'auto' and each row is its own grid, 'ZIP · 1,1 MB' is wider than 'HTML · EN' and moves the chip and link columns sideways row by row (visible at 1440 and 1024). Fix: Use sm:items-start with sm:grid-cols-[2rem_minmax(0,1fr)_8.5rem_7.5rem]. Give the chip wrapper sm:justify-self-start sm:mt-0.5 and the link sm:self-start sm:-mt-2.5, so the icon, title, chip and action share the title line as in recipe 6.11.
- [medium] workshop-detail-content.tsx, sections 7 and 8 (Für wen / Danach kannst du / Das brauchst du / Nicht Teil dieses Workshops) via SectionHead: The hierarchy is weak. Every h2 renders at 36px (text-fluid-h2), including four short list blocks, so the lower half reads as a stack of headlines. The h1 is only 1.44x the h2 (52 vs 36px) and the lab question is also a 36px h2. Fix: For the two-column blocks, keep the Kopflinie but render the title as text-fluid-h3 (24px): add size="sm" to SectionHead, or use a local <h2 className="text-fluid-h3 font-bold">. Optionally set the cover h1 to lg:text-display (keep text-fluid-h1 below lg so the phone start button stays above the fold) with max-w-[16ch], as in blueprint 6.9.
- [medium] workshop-detail-content.tsx Route stations/agenda (line ~236 stations; Route mode="description"): The Route is purely descriptive: every station is filled and nothing points to the lab below. Blueprint 6.10 and 7.2 ask for the station that matches the lab to get the inset 'here' square and the caption link 'unten ausprobieren'. As built, the grey lab band appears without an explanation. Fix: Pass current={index of the agenda item the lab mirrors} (W03: 1 'Die falsche Antwort', W01: 0, W02: the 'Entscheiden' station). Add a caption link <a href="#<lab id>" className="inline-flex min-h-11 items-center gap-1.5 text-caption font-semibold underline decoration-border underline-offset-4">Unten ausprobieren ↓</a> (EN 'Try it below ↓') under the Route. If description mode cannot show a 'here' station, ask the Route owner for a highlight prop and note it in the change log.
- [medium] workshop-detail-content.tsx cover caption <p className="mt-5 max-w-[70ch] text-caption ..."> (line ~344): The caption wraps at 70ch (about 500px) and leaves 'Anmeldung' orphaned on a second line at 1440 and 1024 (W03 DE), with 800px of empty band beside it. Fix: Change max-w-[70ch] to max-w-[48rem]. Render each coverFacts item as <span className="whitespace-nowrap"> joined by ' · ', so wraps only happen between facts.
- [medium] src/lib/workshops-data-readiness.ts decisionLab.title (line 130), src/lib/workshops.ts decisionLab.title (line 362); lab h2 max-w-[20ch] in workshop-decision-lab.tsx line 431: The lab titles use the staged fragment pattern the voice spec bans: '100 Euro plus 20 Euro. Wirklich 120?' and '1.050 Stück. Wer bekommt sie?'. At 20ch the W03 title also breaks '20 / Euro', separating the number from its unit, at every width. Fix: Rewrite as plain questions, e.g. W03 'Die KI meldet 120 Euro Endbestand. Übernimmst du die Zahl?' and W01 'Wie verteilst du 1.050 Stück auf drei Standorte?' (EN to match). Use U+00A0 between number and unit ('20 Euro'). Set the h2 to max-w-[24ch]. Data owner: the copy stream. List it in the integrator notes.
- [medium] workshop-copy.ts detail.outcomesHeading DE 'Danach kannst du' + workshop.outcomes items: The heading and the items don't agree grammatically: 'Danach kannst du' is followed by bullets that start with 'Du erkennst…', 'Du schreibst…' ('Danach kannst du / Du erkennst'). EN 'After this you can / Write down…' is fine. Fix: Rename the DE heading to 'Nach dem Workshop' (it fits the Du-sentences). Also make the cover label consistent: keep 'Du gehst mit' only once, in the cover dl, and drop the repeated caption 'Du gehst mit: …' under the outcomes list (lines ~559-563).
- [medium] Der Fall + provenance + access note (workshop-detail-content.tsx lines ~440-460 fictional note, ~613-620 provenance.note; data in workshops-*.ts caseStudy.limitations[0], accessNote, provenance.note): Repetition that reads like padding. W03 says the case is invented 5 times: 'Erfundener Fall' caption, 'eingefrorener Übungsstand', 'FOLDLINE und alle Zahlen sind … erfunden', the first gap bullet, and 'Daten: erfunden' plus provenance.note. It repeats 'kostenlos, ohne Anmeldung' 2x, 'Material auf Englisch' 4x (cover, Material caption, access note, chips) and 'KI-Antworten im August 2026 aufgezeichnet' 2x. The live date also appears twice in the provenance footer. Fix: Keep 'Erfundener Fall' only as the SectionHead caption and in the provenance line. Remove the <p> fictional note under the narrative and the gap callout's first bullet. In provenance, drop provenance.note sentences that restate the caption line (keep only the PostgreSQL 16 run detail). Cut the access note to what is new: 'Einführung auf Deutsch.' Drop 'Material auf Englisch' from the Material SectionHead caption because the chips already say EN.
- [low] workshop-detail-content.tsx station captions (line ~238 join(' · ')) rendered by src/components/werk/route.tsx: At 1024 the 8-station W03 Route gives each station about 120px, so captions break as '17 Min. ·' / 'Mitmachen' and '10 Min. ·' / 'Schreiben' with the dot dangling. Most station labels also wrap to two lines, so the agenda looks cramped. Fix: Build the caption as two lines (minutes, then activity plus flags) joined with '\n', and have Route render the caption with whitespace-pre-line (integrator note for route.tsx). Alternatively, render the Route vertically below lg when stations.length > 6.
- [low] workshop-detail-content.tsx MaterialRow notes (line ~136); material labels in workshops-*.ts: The minutes appear twice: the title 'Interaktive Demo · 10 Min.' sits over the caption '10 Min. · optional', and the same happens for 'Browserlabor · 12 Min.'. 'Readiness-Kit · .zip' repeats the 'ZIP · 1,1 MB' chip. Fix: In notes, include copy.minutes(material.minutes) only when !material.label.includes(`${material.minutes} Min`) (EN 'min'). Or drop the minutes from the labels in the data. Rename the label to 'Readiness-Kit'.
- [low] workshop-decision-lab.tsx facts <ul> (lines ~438-446) and config.facts strings: Facts render as a single run of bold text ('Endbestand 100 €   Veränderung +20 €   KI-Antwort 120 €'), with label and value at the same weight. At 1440 they wrap mid-pair ('Endbestand / 100 €'). The recipe asks for a hairline fact table with the label above the value. W01's 'Nachfrage p50 1.180' is also unexplained jargon. Fix: Change facts to {label, value}[] (or split on the last space). Render <dl className="grid grid-cols-3 border-t border-hairline pt-3"><dt className="text-label text-muted-foreground"> / <dd className="mt-1 text-[1.25rem] font-bold tabular-nums">. Rename 'Nachfrage p50' to 'Geschätzte Nachfrage'.
- [low] workshop-detail-content.tsx QuestionCard className 'order-last mt-8 max-w-[42rem] sm:order-none sm:mt-6' (line ~308); globe mask in CoverBand: At 1024 the globe's Mennige Germany outline sits directly against the q-card's right border (q-card ends at x≈695, Germany starts at x≈720). The two Mennige marks crowd each other and read as one clump. Fix: Use max-w-[42rem] md:max-w-[34rem] lg:max-w-[42rem] on the QuestionCard. Integrator: alternatively, show the globe only from lg (hidden lg:block) in the shared CoverBand.
- [low] workshop-detail-content.tsx cover <dl className="mt-1 flex max-w-[70ch] flex-wrap gap-x-6 gap-y-1 ..."> (line ~347): 'Du brauchst' and 'Du gehst mit' sit inline in W01/W03 but stack in W02 (a longer need), and the labels and values don't align between the pages. The mt-1 against the caption above makes the three caption lines feel cramped. Fix: Use <dl className="mt-3 grid max-w-[48rem] gap-y-1 text-caption text-muted-foreground sm:grid-cols-[auto_minmax(0,1fr)] sm:gap-x-3"> and make each pair <div className="contents">, so the labels form one aligned column on every workshop.
- [low] workshop-decision-lab.tsx COPY.de.noScript / COPY.en.noScript (lines ~47, ~64); copy.firstLegend 'Deine erste Entscheidung': The noscript text says 'Du kannst den Kurs … lesen' / 'read the course', but this is a workshop page. The legend 'Deine erste Entscheidung' / 'Your first decision' implies more decisions, but the lab has one (the kicker says 'Entscheidung 01'). Fix: noScript DE: 'Diese Übung braucht JavaScript. Die Seite und das Material funktionieren auch ohne.' EN: 'This exercise needs JavaScript. The page and the materials work without it.' Change the legend to 'Deine Entscheidung' / 'Your decision'.

### critique:B3-kurse-hub

Verdict: Mostly there. /kurse and /en/kurse now read as calm and editorial rather than brutalist. The paper ground, ink-only H1, Kopflinie heads, joined square goal tabs, flat next-step sheet with the page's single Mennige button, the Beton workshop band and the graphit footer all follow the Werkzeichnung direction. I found no pastel fills, offset shadows, mono-uppercase eyebrows or orange left rules. axe reports 0 violations on DE and EN at 1440 and 390. There is no horizontal overflow at 390, 1024 or 1440 (scrollWidth equals clientWidth). The webfont loads, and the H1 is 700 weight at -0.012em.

What still looks unfinished is the ledger's layout mechanics:
- The duration/level column and the action column sit at different x positions in the two groups.
- At 1024 the promise text is squeezed to about 340px by a 260px action link.
- The access state is printed twice in every foundation row.
- Row titles, group heads, the Route head and the sheet title all use the same 22.7px size, so the hierarchy is flat.
- In a deployment without accounts (the dev server and probably previews), the Route has no current station, while the sheet next to it recommends a course that is not on the Route.

In the copy, ten row promises in a row start with "Nach dem Kurs", which reads as a template, and "kostenlos" appears four times.

Screenshots are in /tmp/claude-0/-home-user-platform/614e303c-f8f0-55ca-be18-13442a9af90b/scratchpad/impl/B3-kurse-hub-critique/: {de,en}-{1440,390,1024}.png, section crops c-de-1440-*.png and c-de-390-*.png, and de-1024-atlas.png.

- [high] src/app/kurse/course-ledger-row.tsx, the row grid div `grid min-w-0 grid-cols-[2.75rem_minmax(0,1fr)] ... lg:grid-cols-[3.5rem_minmax(0,1fr)_12rem_auto]`: Each <li> has its own grid with an `auto` action track, so the columns move with the length of the action label. At 1440 the duration/level column starts at x=782 in Grundlagenpfad but x=952 in Technikkurse, and the action column at x=998 vs x=1168. At 1024 the long 'Hier nicht verfügbar · Kursübersicht' track (~260px) plus the 12rem meta column squeeze the promise to ~340px. The promise then runs 4-5 lines and the row becomes a tall ragged block. Fix: Use fixed tracks and add the 4th column only from xl. Change to `lg:grid-cols-[3.5rem_minmax(0,1fr)_15rem] xl:grid-cols-[3.5rem_minmax(0,1fr)_10rem_15rem]`. Put the meta `dl` and the action in one right-hand cell below xl: data-course-meta `lg:block lg:pt-3`, and data-course-action `lg:col-start-3 lg:row-start-1 lg:self-end xl:col-start-auto xl:row-start-auto xl:self-auto xl:justify-self-end`. Then pin the widths so both groups line up: add `xl:w-[15rem] xl:text-right` to the action wrapper and `w-full` to its link.
- [high] src/app/kurse/course-ledger-row.tsx, the data-course-access-label <p> plus courseAction(); src/app/kurse/learning-atlas.tsx ATLAS_COPY.unavailableAction: The access state is printed twice in every foundation row: a bold caption 'Hier nicht verfügbar' under the promise, and then the action 'Hier nicht verfügbar · Kursübersicht →'. With accounts on, it becomes 'Lernkonto nötig' plus 'Kurs starten · Lernkonto nötig'. That is four lines of repeated state across four rows, which is noise, and the bold caption at 12px/600 is a third text style in the row. Fix: Keep the state once, as data. Move the access word into the meta dl as a third item: `<dt className="sr-only">Zugang</dt><dd>{access==='account-required'?copy.accountRequired:copy.unavailable}</dd>`. Hide the data-course-access-label <p> from lg up with `lg:sr-only` (it stays visible below lg, where there is no meta column). Shorten the visible action to the verb only ('Kursübersicht' / 'Kurs starten') and move the state into the existing sr-only span after the label, so the accessible name still contains the pinned strings 'Hier nicht verfügbar · Kursübersicht' and 'Lernkonto nötig'.
- [medium] src/app/kurse/learning-atlas.tsx, openDefault/nextCourse logic (around l.231-262) and the Route map (StationSquare): When the account feature is off (dev server, and any preview without accounts), openDefault switches the sheet to the Claude Course. The Route shows the four foundation courses, so no station gets aria-current or the inset 'current' square: all four are outlined and dashed. The one Mennige button then points at a course that is not on the path next to it. That is the first thing a visitor sees after the tabs, and it contradicts the Route's own story. Fix: Keep the sheet on the path course. Drop the `openDefault` override, so nextCourse = pathNextCourse and the first path course gets the inset square. Show the open course only through the existing openAlternative text link ('Offene Alternative ohne Lernkonto: Claude Course'). If a spec pins 'Offener Einstieg ohne Lernkonto' in the sheet kicker, show it only when `!pathCourses.some(c=>access[c.slug]!=='unavailable')` and still mark pathCourses[0] as `current`. Note for the integrator: check route-kurse-hub.spec and mobile-access-disclosure.spec.
- [medium] src/app/kurse/course-ledger-row.tsx h4 `text-fluid-h3`; learning-atlas.tsx group head h3 `text-fluid-h3`, Route h3, next-proof h3: Four levels render at the same 22.72px/700: the group head 'Grundlagenpfad', every row title, the Route head 'Ich nutze KI im Job' and the sheet title. Row titles even look heavier than the group head above them, because they sit alone with more white space. With a flat hierarchy the ledger reads as one long list instead of two groups. Fix: Row title (h4): change `text-fluid-h3` to `text-[1.25rem] leading-snug` (20px, which spec 6.6 allows). Group head h3: add a Kopflinie and a size step. Replace the wrapper `border-b border-foreground pb-3` with `border-t-2 border-foreground pt-3 pb-2`, and give the h3 `text-[1.625rem] leading-tight`. Rows are already divided by hairlines, so the ink rule below the head can go. This also removes the double rule stack under 'Alle Kurse'.
- [medium] src/app/kurse/learning-atlas.tsx, Route <h3>{goal.label}</h3>: The Route head repeats the selected tab label word for word ('Ich nutze KI im Job' directly under the ink-filled tab 'Ich nutze KI im Job'), so the eye reads the same phrase twice in 60px. Fix: Make the h3 name the path: `{copy.pathHeading(pathCourses.length)}` with DE 'Dein Pfad · 4 Kurse' and EN 'Your path · 4 courses'. Keep goal.summary as the line below it, and keep goal.label only in an sr-only span inside the h3 so the heading stays unique per goal.
- [medium] src/app/kurse/learning-atlas.tsx, Route station <Link> `flex ... flex-wrap items-baseline justify-between`: At 1440 the title sits at x=190 and the duration/state is pushed flush right to x=798, which leaves a 400-500px gap. Each meta line ends at a different point ('ca. 46 Min. · offen' vs 'ca. 5 Std. Lektionen, 12 Std. mit Übungen · offen'), so the right edge looks ragged and the eye can't pair a time with its title. Spec 6.10 puts label and time stacked to the right of the station. Fix: Stack them. Change the link classes to `group flex min-h-11 min-w-0 flex-col items-start gap-0.5 py-2`, drop `justify-between`, and keep the meta span as `text-caption text-muted-foreground tabular-nums`. Adjust the connector to `top-[1.375rem] -bottom-[1.375rem]` if the station centre moves. Also cap the Route column at `max-w-[34rem]`.
- [medium] src/lib/courses/course-hub-copy.ts COURSE_PROMISES (de and en): All ten promises open with the same frame, 'Nach dem Kurs (kannst du / weißt du / hast du) …' in DE and 'After this you …' in EN. Read down the ledger, this is exactly the templated rhythm the voice rules warn about. Each promise also repeats in the sheet. Fix: Say the frame once and start each promise with the concrete verb. Add a caption under 'Alle Kurse': DE 'Jede Zeile sagt, was du nach dem Kurs kannst.' / EN 'Each row says what you can do afterwards.' Then rewrite, for example: KF 'Du weißt, welche Daten in welches KI-Tool dürfen, und prüfst eine KI-Antwort, bevor sie in eine Mail geht.'; Claude 'Du schreibst Prompts mit Kontext, Beispielen und festem Ausgabeformat und legst eine CLAUDE.md für dein Projekt an.'; DS 'Du hinterfragst eine Kennzahl wie „92 % Accuracy“ und erkennst einen zu früh gestoppten A/B-Test.' EN likewise: 'You know which data may go into which AI tool …'. Update the catalog-copy.test promise check if it asserts the prefix.
- [low] src/app/kurse/course-ledger-row.tsx, the `inPath` <p> 'Teil deines Pfads' (`lg:not-sr-only lg:-mt-1`) and the text column stack: On desktop a foundation row stacks six tiers in the text column: title, 'Teil deines Pfads' (pulled up tight with -mt-1), promise, bold access label, demo link, and on mobile the level caption above the title. The caption repeats on all four rows while the ink square marker already carries the meaning. The result is cramped and busy compared with the calm technical rows. Fix: Keep 'Teil deines Pfads' sr-only at every width: remove `lg:not-sr-only lg:-mt-1`. The ink square before the number is the visible signal (spec 6.6), and the group caption can say it once: sections.spine.eyebrow becomes '4 Kurse · DE + EN · fester Ablauf · ■ = in deinem Pfad'. Together with the access fix above, rows drop to title, promise and links.
- [low] src/app/kurse/course-ledger-row.tsx, the data-course-source <p> (mono repo and commit links): The identical mono line 'Mavengence/interactive-courses  Commit #0e5dfd3' repeats in all six Technikkurse rows: six dotted-underline mono strings that look like leftover chrome, and twelve extra 44px targets. Spec 7.3 wants provenance as a caption line; one line per group is enough when every course shares the repo and commit. Fix: When every course in the group has the same sourceHref origin and sourceCommit, render the source once in the group head caption: tracks.ts deeper caption 'Quellcode auf GitHub' becomes a <p className="font-mono text-caption"> with links 'Mavengence/interactive-courses' and '#0e5dfd3'. Omit data-course-source from the rows in that case, and keep it per row only for courses whose source differs. Keep the accessible names ('Quellcode, <Kurs>' can become 'Quellcode der Technikkurse').
- [low] src/lib/courses/course-hub-copy.ts kicker / heading / accessBody / workshopsNote: 'kostenlos' appears four times in about three screens: the kicker ('10 Kurse · kostenlos · …'), the H1 ('Kostenlose KI-Kurse …'), 'Material kostenlos und ohne Konto.' and 'Alle Kurse sind kostenlos.' The H1 already says it. Fix: Kicker DE `${count} Kurse · Deutsch und Englisch` / EN `${count} courses · English and German`. workshopsNote DE 'Material ohne Konto.' / EN 'No account needed.' Keep 'Alle Kurse sind kostenlos.' in Kosten und Konto, where it answers the heading. Update the page.test kicker assertion if one pins the string.
- [low] src/app/kurse/page.tsx, 'Kosten und Konto' section links and section order: The section ends with 'Über mich →' and 'KI-Check →'. 'Über mich' has nothing to do with cost or accounts, and the KI-Check link duplicates the hero link. The paper section also comes after the Beton band, so the page ends paper → Beton → paper → graphit, and the account note reads like an afterthought after the workshop call. Fix: Replace the links with one relevant action: `localizeHref('/konto', locale)` labelled DE 'Lernkonto anlegen' / EN 'Create a learning account'. Remove aboutMe and aiCheck from the copy. Move the <section aria-labelledby="kurse-access-heading"> above the workshop band (inside the max-w-[1180px] container after the atlas, `mt-16 lg:mt-20`), so the page ends ledger → account note → Beton band → footer.
- [low] src/app/kurse/page.tsx, workshop band inner grid `gap-4 ... lg:grid-cols-[minmax(0,1fr)_auto] lg:items-center`, and copy.workshopsBody: The band works, but the body is set in a mid-sentence numeral ('In jedem der 3 Workshops', 'Each of the 3 workshops'), which reads like a template variable. The caption is 12px at 60ch directly under 16px text with mt-1, so it looks crowded. Fix: Spell the count out for 2-12: add a numberWord(locale,n) helper in course-hub-copy.ts (DE 'drei','vier'; EN 'three','four') and use `In jedem der ${numberWord('de',count)} Workshops …`. Change the note to `mt-3`. Optionally add `lg:items-end`, so the outline button aligns with the last text line instead of floating at mid-height.
- [low] src/app/kurse/course-ledger-row.tsx, the number span `grid-cols-[2.75rem_minmax(0,1fr)]` and the `data-course-level-label` <p> below lg: At 390 the number column plus gap takes 56px, which leaves the promise about 285px wide (4-6 lines per row). The 'Einstieg · ca. 1 Std. 40 Min.' caption above the title works as a small eyebrow, which spec 6.6 puts after the promise instead. Fix: Below sm, put the number inline: `grid-cols-[2rem_minmax(0,1fr)] gap-x-2 sm:grid-cols-[2.75rem_minmax(0,1fr)] sm:gap-x-3`, and use `gap-1.5` on the number span. Move the level/duration <p> after the promise: render it below the promise with `mt-1 lg:hidden` instead of above the h4.
- [low] src/lib/courses/course-hub-copy.ts metadataDescription (de/en); checkLabel (en): The meta description still describes the old UI ('Jede Karte nennt Umfang, Zugang und Quellstand' / 'Every card states scope, access, and source revision'), but there are no cards now, and 'Quellstand' is jargon. The EN 'Map it in five minutes' is an odd, slightly slogan-like phrase. Fix: DE: 'Zehn kostenlose KI-Kurse auf Deutsch und Englisch, dazu Workshops mit Material zum Herunterladen. Jeder Kurs nennt Dauer, Stufe und ob du ein Konto brauchst.' EN: 'Ten free AI courses in English and German, plus workshops with downloadable material. Each course lists duration, level and whether you need an account.' EN checkLabel: 'Find out in five minutes'.
- [low] src/app/kurse/page.tsx hero container `pb-14 ... lg:pb-20` plus atlas `mt-8 sm:mt-14`: At 1440 there are about 80px of empty space between the KI-Check link and the 'Womit fängst du an?' Kopflinie, while the hero uses only the left 46rem of a 1180px column. The first viewport ends just under the Route, and the Mennige button (y≈905) falls below the 900px fold on a 1440x900 screen. Fix: Tighten the gap: hero wrapper `lg:pb-20` becomes `lg:pb-10`, and the atlas section `sm:mt-14` becomes `sm:mt-10`. That lifts the sheet's 'Kurs starten' button above the 900px fold without crowding the hero.

### critique:B5-demos

Verdict: The hub and the page chrome have mostly made the move. The hero is paper and calm, the stats come from data, the filters are square with ink fill, there are no offset shadows, and the redrawn schematic previews are the best part of the work. Nothing overflows sideways at 1440, 1024 or 390. axe finds no violations on /demos or /en/demos.

The detail pages still do not read as Werkzeichnung. Two things undo the calm frame:
1. **Old-look engine interior:** the first thing you see is the engine, and it still has the brutalist look: mono-uppercase labels, two-tone orange headlines, orange left rules and an empty "press play" state. axe reports 11 serious contrast failures on /demos/agent-pipeline (1440) and 4 on /en/demos/excel.
2. **Legacy banner:** a mono-uppercase banner with an orange rule sits above the page, and it contradicts the stage shown in "Im Kurs" (Stufe 4 against Stufe 5).

The hub grid also departs from blueprint 6.14. It uses bordered bento sheets with chip boxes inside, where the blueprint asks for uniform tiles with no borders. Hero and tall tiles leave large empty Beton bands. Hyphenated two-sentence titles break badly, and clamped descriptions end mid-sentence.

Screenshots and axe.json are in /tmp/claude-0/-home-user-platform/614e303c-f8f0-55ca-be18-13442a9af90b/scratchpad/impl/B5-demos-critique/. It is not ready to ship until issues 1 to 4 are fixed.

- [high] /demos/agent-pipeline, /en/demos/excel (and every dark engine): src/components/demos/agent-pipeline-demo.tsx, excel-demo.tsx, src/lib/demo-tokens.ts: axe reports serious color-contrast failures inside the engines. On agent-pipeline (1440) there are 11 nodes: #53514f on #070606 at 2.56:1, #6a6865 at 3.64:1, and #4c4b49 on #181818 at 2.03:1 (390). On Excel there are 4: #b7d8c6 on #107c41 at 3.42:1, #9f9d98 on #f7f4ed at 2.46:1, and Mennige #b73a15 on #0b0908 at 3.43:1. This breaks the Lighthouse a11y 1.00 contract on the pages whose restyle this is. Fix: In demo-tokens.ts (or inline in the engines), raise the dim text on dark surfaces to at least #9a978f (about 6:1 on #070606) and the step-number spans to the dark-fg token. On dark grounds, replace Mennige text with the light accent #e07050 (only as text, never with white on it). In the Excel ribbon, replace #b7d8c6 on #107c41 with #ffffff, or drop the green ribbon entirely (it is app chrome, removal list #14). Replace #9f9d98 on paper with text-muted-foreground (#4f4640).
- [high] Engine interiors: agent-pipeline-demo.tsx ('MULTI-AGENT WORKFLOW', 'Vier Agenten. Ein Memo.', 'AGENT.LOG', border-l orange on the log, '→ MEMO ERSCHEINT NACH PIPELINE-ABSCHLUSS'); prompt-scanner-demo.tsx ('COMPLIANCE-SANDBOX', 'Prompt-Scanner für DSGVO & IP.', 'GRENZFALL:', orange-filled 'ERKANNT' tab, orange left rule on 'MARKIERT'): The first 700px under the new hero are the old brutalist look: mono-uppercase eyebrows in orange, two-tone headlines, orange left rules and many Mennige groups in one section. The copy is the banned kind. 'Vier Agenten. Ein Memo.' is a count-first slogan, 'GRENZFALL: WAS PASSIERT, WENN…' is a colon reveal, and 'für DSGVO & IP' contradicts the new 'Personendaten markieren' rename. At 390 the pipeline loads at 'Schritt 0 / 14' with '// warten auf pipeline start' and an empty memo pane, which violates principle 6, final state first. Fix: This is a scoped engine pass, not only shadow removal. Replace every `font-mono … uppercase tracking-[…] text-brand-orange` label with `text-label text-dark-muted` in sentence case. Make headlines one colour: 'Memo aus vier Agentenschritten' and 'Personendaten im Prompt markieren'. Remove `border-l-2/3 border-brand-orange` on the log and the result row. Make the active tab ink or paper fill instead of orange. Set the agent pipeline's initial step to steps.length (memo visible), with 'Neu abspielen' as the optional replay. Allow one Mennige mark per engine (the current step).
- [high] src/app/demos/[slug]/page.tsx line 124 `<ResourceContextBanner nodeId=…>` (component in src/components/learning/resource-context-banner.tsx): A mono-uppercase strip with an orange left rule ('STUFE 4: UMSETZEN · DIESES PRAXISBEISPIEL GEHÖRT ZU DIESER STUFE. PASSEND ZUM AI-NATIVE ARBEITSKURS.') sits above every DE detail page. It is removal items 4 and 6, and it is DE-only, so DE and EN differ. It also contradicts the page: the banner says Stufe 4 while 'Im Kurs' says 'Stufe 5: Gestalten' for agent-pipeline. Its copy is filler. Fix: Stop rendering it on demo pages (delete line 124 and the import). The 'Im Kurs' block already carries course, lesson and stage. Then fix the stage mismatch at the data source (the learning-graph node for demo:agent-pipeline versus the demo's courseLink stage) so both read the same stage.
- [high] src/components/demos/demo-tile.tsx root Link class `border border-hairline bg-card` + `Chip` meta chips; demo-grid.tsx line 330 `gap-6`: Blueprint 6.14 asks for tiles with no borders, separated by whitespace, with the preview as the only box. The implementation stacks a bordered tile, a bordered Beton band, bordered objects inside the drawing and two bordered chips. That is four box levels and reads as a card grid, which is the box-in-box density the removal list (#5) targets. The 12 heavy chip boxes are most of the visual noise in the grid. Fix: Tile root: remove `border border-hairline bg-card` and use `group flex min-w-0 flex-col`. Preview: keep `bg-inset`, drop `border-b`, and add `transition-colors group-hover:bg-[#dcdad6]` for the hover. Text block: use `px-0 pt-4 pb-0`. Replace the two `<Chip>` elements with one caption line: `<p className="mt-3 text-caption text-muted-foreground">{evidenceLabel} · {levelLabel}</p>`. Grid: `gap-x-6 gap-y-12`.
- [medium] src/components/demos/demo-tile.tsx previewSizeClass: s-hero `min-h-56 flex-1 lg:min-h-72`, s-tall `min-h-44 flex-1`: At 1440 the hero tile 01 has a Beton band about 420px tall with a 150px drawing floating in it. The tall tile 06 (RAG) has about 260px of empty grey above a bottom-weighted drawing. At 1024 the same bands look like missing images. Empty grey fields read as unfinished rather than calm. Fix: s-hero: `h-56 lg:h-72` (no flex-1), and let the text block take the slack with `flex-1`. s-tall: `h-44` like the others, with `flex-1` on the text block. Better, adopt the blueprint's uniform 3/2/1 grid: drop `lg:row-span-2` and the `s-tall`/`s-hero` spans in tileSizeClass so every tile is 1 column with a fixed `aspect-[4/3]` preview. That also scales cleanly as tiles are added or removed.
- [medium] src/lib/demos.ts title + titleKicker (all 12), rendered in demo-tile.tsx line 120 and demo-detail-layout.tsx line 151: Every heading is two full-stop sentences in bold ('Cost & Drift. Kosten und Drift ablesen.', 'Fine-Tuning-Playground. Basismodell vs. Domäne.'). It reads as a slogan pattern repeated twelve times, and several are tautologies ('Annahmen-Rechner. Annahmen einzeln ändern.', 'Cost & Drift. Kosten und Drift…'). The hyphenated compounds split into ugly fragments on narrow tiles: 'Fine-Tuning-/Playground.', 'Annahmen-/Rechner.', 'LLM-/Qualitätsmessung.', 'Vertrags-Assistent. Antworten / mit Fundstelle.'. The detail H1 breaks as 'Agent-Pipeline. Ein / Memo in vier Schritten.' with an orphaned 'Ein'. Fix: Render only `title` (without the full stop) as the H3/H1, and move `titleKicker` into the kicker line or the lead. Alternatively, merge each pair into one plain name: 'Kosten und Drift im Betrieb', 'Feintuning gegen Basismodell', 'Annahmen-Rechner', 'Rechnung zu SAP prüfen'. Add `text-balance` to the detail H1 (line 151 lacks it) and `hyphens-manual` to both. Replace 'vs.' with 'gegen'.
- [medium] src/components/demos/demo-tile.tsx descriptionClampClass `line-clamp-4`: Clamped descriptions end mid-clause with an ellipsis on 5 of 12 tiles at 1440: 'Danach prüfst du den Stil, Quellen, Freigabe und…', 'Daneben…', 'In einem Fall…', 'Die Treffer sind Hinweise und…', 'wo ein Budget-Alarm…'. Truncated copy looks broken and hides the concrete detail the rewrite added. Fix: Remove the clamp (`descriptionClampClass` returns '') and cap every `description` in demos.ts and demos-localization.ts at about 150 characters, or at two sentences that fit in 4 lines at 280px. If a clamp must stay as a guard, use `line-clamp-5` with copy written to fit.
- [medium] src/components/demos/demo-gallery-previews.tsx (Excel table and agent-pipeline route previews) at 390: On phones the drawings truncate their own data: the Excel table shows 'We…', 'No…', '91…', '78…', and the route labels read 'Reche… Synth… Fehler… Redak…'. A schematic with ellipses looks broken, and 'final state visible' fails. Fix: Below sm, drop the Region column (for example `hidden sm:table-cell`) and show KW + Umsatz in full. For the route, stack the labels under the stations with `text-[0.75rem] whitespace-nowrap` and use short labels (Suche, Synthese, Kritik, Text), or show numbers only and give the full label to the current (Mennige) station.
- [medium] src/app/demos/page.tsx lines 174/222 and src/components/demos/demo-detail-layout.tsx lines 120/145/178/308 `max-w-[75rem]`: Three different left edges at 1440. Page content starts at x=120 (75rem), the nav wordmark at x=144, and the footer at x=168 (max-w-6xl with px inside). The hero, the grid and the footer do not line up, which undermines the ruled, drafted feel. Fix: Use the site column: replace `max-w-[75rem]` with `max-w-6xl` in both files, so the content edge becomes 144 + 24 and matches the nav. Tell the integrator that footer.tsx pads inside its max-w (px-4 sm:px-6 inside `max-w-6xl`) while pages pad outside; one convention should win site-wide.
- [medium] src/components/demos/evidence-badge.tsx note span + src/lib/demos-copy.ts syntheticDataLabel: The evidence line repeats itself. On the agent pipeline it reads 'Aufgezeichnete Spur ↓ · Aufgezeichnete Beispielspur; keine Live-Agenten im Browser.', then 'Ausführung: Aufgezeichnete Spur' and 'Aufbau: … · aufgezeichnete Spur' below. That makes five 'aufgezeichnet' in one screen. The note uses a semicolon plus a trailing negation, which the voice rules ban. At 390 the note wraps onto its own line starting with a stray '·'. The down arrow reads as 'scroll down', not as a disclosure. Fix: Rewrite the notes as one positive fact, for example 'Die Spur wurde am 2026-05-12 mit Beispieldaten aufgenommen.' or 'Regeln laufen lokal mit Beispieltexten.'. Render the separator only from sm up: `<span aria-hidden className="hidden sm:inline">· </span>`. Replace the ArrowGlyph down with a plus/minus or chevron pictogram at size-4. Drop 'aufgezeichnete Spur' from the Aufbau value.
- [medium] src/components/demos/demo-detail-layout.tsx lines 178-300 ('Worum es geht' col-span-7 / 'So läuft dieses Beispiel' col-span-5): The right column is a 10-row table (Aufbau … Log) plus tags plus 'Im Kurs', about 900px tall. The left column ends after about 350px, leaving a large dead area. Blueprint 7.6 specifies four rows. Rows like 'Log: vollständig', 'Output: Memo + Kritik' and 'Agenten: 4 spezialisiert' are thin, and the tags 'Multi-Agent · Opus 4.5 · Trace' are jargon. 'Was du prüfen solltest' has a single item. Fix: Cut the dl to the four blueprint rows (Daten, Ausführung, Externe Aktionen, Abbruch). Move meta rows to `hidden` or drop them, and drop the tag chips. Move 'Im Kurs' under the left column, or into the footer section (see the next issue). Give 'Was du prüfen solltest' 2 to 3 items per demo (riskNotes in demos-copy.ts). Change the grid to `lg:grid-cols-12` with 7/5 and `items-start`.
- [low] src/components/demos/demo-detail-layout.tsx 'Im Kurs' block + footer section line 308: The course is announced three times: 'Im Kurs: AI-Native Arbeitskurs … Zur Lektion', then 'Weiterlernen / AI-Native Arbeitskurs / [AI-Native Arbeitskurs öffnen]'. The footer heading and the button say the same words. That is redundant, and it spends the page's one Mennige button on something already linked. Fix: Merge them. Keep 'Im Kurs' only in the footer left column: kicker 'Im Kurs · Modul 4, Lektion 2', H3 = lesson title, Mennige button 'Zur Lektion'. Delete the separate 'Im Kurs' block and the 'Weiterlernen' duplicate heading. Make the right column's secondary button name its target: 'Lieferverzug mit Freigabe ansehen' instead of 'Weiter'.
- [low] src/app/demos/page.tsx StatRow at 390 (components/werk/stat-row.tsx `grid grid-cols-2 … sm:grid-cols-3`): With three stats on a phone, the third ('0 Echte Außenaktionen') drops alone into a second row, and its caption wraps to 3 lines, so the row looks broken. The number 12 is also stated three times in the first screen: the kicker 'Praxisbeispiele · 12', stat '12 Praxisbeispiele', and the section caption '12 Praxisbeispiele'. Fix: In StatRow, use `grid-cols-3` for exactly 3 stats (or grid-cols-1 with hairline rows below sm) and `text-num-md` below sm. Drop the count from the hero kicker (use just 'Praxisbeispiele'), or replace the '12' stat with a different derived fact, for example the number of industries.
- [low] src/components/demos/demo-detail-layout.tsx 'Arbeitskontexte' chips (Strategie, Corporate Development, Investment): These static labels are drawn as 44px-tall bordered boxes, identical to the hub's filter buttons. They look clickable but do nothing, which is a false affordance, and they add another boxed layer to an otherwise ruled section. Fix: Render them as the 6.4 meta chip (`inline-flex h-7 items-center border border-border px-2.5 text-label text-muted-foreground`) or as a caption line `text-caption text-muted-foreground` joined with ' · '. Better still, make each a real link to `/demos?kontext=<slug>` and keep the 44px target.
- [low] src/app/demos/page.tsx hero grid (right aside 'Was du an jedem Beispiel prüfst'): At 1024 the check list is bottom-aligned against the lead. Its heading starts about 110px below the H1 and floats mid-column, so the two columns share no baseline. At 1440 it tops out beside the H1 while the lead runs 4 lines, which is inconsistent across breakpoints. The lead ends with a negation ('Kein Beispiel verbindet sich mit einem Firmensystem.'), which the 0-actions stat already states. Fix: Put `items-start` on the hero grid and `lg:pt-[calc(var(--text-label)+0.75rem)]` on the aside so it aligns with the H1 top. Alternatively, per the blueprint, place the list under the lead in the same column (`mt-6 max-w-[56ch]`). Cut the last lead sentence.

### critique:B4-course-landings

Verdict: The four native landings (/ki-fuehrerschein, /eu-ai-act-kurs, /ki-und-gesellschaft, /ai-native) now read as Werkzeichnung. They have a paper hero, an ink H1, Kopflinie section heads, hairline ledgers and one Mennige button. Axe is clean on all four, there is no overflow at 1440, 1024 or 390, and there is one h1 each. What remains there is polish: headline breaks, orphans, an empty progress bar for visitors who are not signed in, and copy in the data files. The other three landings and every lesson reader were only partly converted and still carry the brutalist look in full:
- **/kurse/open-source/data-science:** matches none of the 7.4 blueprint. It keeps the italic Mennige two-tone H1 at weight 400, the pastel loop diagram with 10px labels, glyph icons, and rounded, colour-coded chapter cards.
- **Claude landing:** the widgets keep 6px offset shadows, pastel rows, pills and three Mennige buttons.
- **Codex and Claude landings:** the lesson ledgers use mono-uppercase orange "LEKTION 10", which wraps onto two lines.
- **Lesson pages:** they stack up to three boxed instruments, with dark bars, offset-shadow buttons and orange left rules, before the lesson text. The title then repeats three times.

Axe finds 2 serious failures, which break the Lighthouse 1.00 contract:
- On the Data Science chapter page (/kurse/open-source/data-science/clean), the active sidebar number has 4.4:1 contrast.
- On the Claude anatomy lesson, the prompt-compare region scrolls but cannot be focused.

Screenshots, crops, report.json and the script are in /tmp/claude-0/-home-user-platform/614e303c-f8f0-55ca-be18-13442a9af90b/scratchpad/impl/B4-course-landings-critique/.

Not ready to ship as "courses lifted" until issues 1 to 6 are fixed.

- [high] src/components/data-science/chapters/de/ch-overview.tsx (+ chapters/ch-overview.tsx EN) and src/components/data-science/ds-v8-scope.css (.ov-*), used by /kurse/open-source/data-science: The whole landing is the old look. It hits removal items 4, 6, 7, 9, 12 and 15 and ignores blueprint 7.4:
- The H1 'Data Science bedeutet, aus Daten Entscheidungen abzuleiten.' mixes ink with an italic Mennige clause, is weight 400 and tracks at -0.024em. There is also a missing space: the DOM reads 'bedeutet,aus'.
- Eyebrows ('— DATA SCIENCE FUNDAMENTALS · V8', '— ERGEBNISSE', '— LEHRPLAN') are mono uppercase with an orange dash.
- The pastel purple, green and pink loop diagram has 10px labels, below the 12px floor.
- The outcome cards carry coloured glyph icons ◇ ○ △ □ ◈ ×.
- The chapter cards are rounded and carry coloured dots and coloured left borders, with 'KAPITEL ÖFFNEN' in mono caps.
- The CTA and 'Nächstes Kapitel' buttons are rounded.
- The stat row is set in mono uppercase. Fix: Rebuild the landing on the course kit, following 7.4:
- **Hero:** use TechnicalCourseHero (or the same classes). Kicker 'Kurs · Data Science Fundamentals · Version 8'. Roman ink H1 in one colour, no <em>/italic span and no text-brand-orange, with a real space after the comma. Stat row as plain `text-caption tabular-nums` ('12 Kapitel · 22 Simulationen · ca. 2 Std.').
- **Loop diagram:** redraw it in ink with square nodes and a dashed 'Rückmeldung' return, plus one Mennige node. Replace `.ov-course-dot`, the pastel strokes and the rounded `.ov-loop-wrap`. Labels at least 12px.
- **Outcomes:** use CourseOutcomeList (drop the glyph icons).
- **Lehrplan:** `grid sm:grid-cols-2 lg:grid-cols-3 gap-px bg-hairline`, cells `bg-background p-6`, no dots, no border-l colour, radius 0. The link reads 'Kapitel öffnen →' in `font-semibold` sentence case.
- **Tools:** a hairline table.
- **Everywhere:** replace every '— XYZ' eyebrow with a Kopflinie section head (`border-t-2 border-foreground pt-4`), and set `.btn`/`.ov-cta-btn` to border-radius 0.
- [high] src/components/imported-courses/claude/hero-orrery.tsx l.223-299, hero-transform.tsx, src/components/widgets/practice/prompt-orrery.tsx l.212/236, src/app/kurse/open-source/claude/page.tsx l.162-176: The first thing below the Claude hero is a box-in-box-in-box widget stack, with these problems:
- **Offset shadows:** `shadow-[6px_6px_0_var(--color-foreground)]` on two panels.
- **Pastel rows:** the selected rows use `bg-brand-orange/10` with orange borders.
- **Mono caps:** 'PROMPT-WERKBANK', 'ROLLE', 'STRUKTURABDECKUNG' and the buttons ('SIMULATION STARTEN', 'STUFE 1 SIMULIEREN') are mono uppercase.
- **Pills:** the steppers and progress bars are `rounded-full`.
- **Too much Mennige:** three Mennige-filled controls sit in one section, on top of the hero's own Mennige button.
- **Section head:** the 'Prompt-Bausteine' h2 has no Kopflinie, and it tracks at -0.025em (l.165).
- **Empty state:** the output panel opens with 'Noch nicht ausgeführt' instead of the final state. Fix: hero-orrery.tsx and hero-transform.tsx:
- **Panels:** remove every `shadow-[…]` so the panels read `border border-foreground bg-card`. Use a single outer box, and use hairlines (`border-b border-hairline`) inside it instead of inner boxes.
- **Rows:** style the on state as `bg-card-hover` plus an ink square marker `size-2.5 bg-foreground`, and the off state as `text-muted-foreground`.
- **Labels:** `font-mono … uppercase tracking-[…] text-brand-orange` becomes `text-label text-muted-foreground`. Keep mono only for the '+18' figures.
- **Buttons:** use the Ink button, `bg-foreground text-card font-semibold rounded-none`, sentence case.
- **Pills:** `rounded-full` becomes `rounded-none`.
- **Empty state:** render stage 3's result on load.

page.tsx l.162:
- Wrap the h2 in the Kopflinie head (`border-t-2 border-foreground pt-4`).
- Change `tracking-[-0.025em]` to `tracking-[-0.015em]`.
- Consider moving the whole lab below the Lehrplan.
- [high] src/components/data-science/ds-chapter-sidebar.tsx l.41-49; src/components/widgets/claude/prompt-compare.tsx (also rewrite-arena.tsx, ai-native/demos/observ-demo.tsx) `.max-h-[150px].overflow-y-auto`: Axe reports 2 serious violations:
- **Contrast:** on /kurse/open-source/data-science/clean the active chapter number is text-brand-orange on `bg-brand-orange/10`, which gives #b73a15 on #edded4 = 4.4:1 at 12px.
- **Focus:** on /kurse/open-source/claude/kurs/anatomy the prompt output has a scrollable region that keyboard users cannot reach (scrollable-region-focusable).

Both break the Lighthouse accessibility 1.00 contract. Fix: - **ds-chapter-sidebar.tsx:** use the active recipe from kurs/lesson-sidebar. Active is `bg-card-hover font-semibold text-foreground` with an ink square marker `before:size-2.5 before:bg-foreground`, and no `border-l-2`. The number span becomes `text-label text-foreground tabular-nums`, dropping `font-mono text-brand-orange`.
- **prompt-compare.tsx and siblings:** add `tabIndex={0} role="region" aria-label="Ausgabe (scrollbar)"` and `focus-visible:outline-2 focus-visible:outline-brand-orange` to each `max-h-[150px] overflow-y-auto` container.
- [high] Codex, Claude, data-infra and operator lesson readers with a course project, e.g. /kurse/open-source/codex/kurs/L01. Files: src/components/course-projects/lesson-mission-frame.tsx l.44/59/75; course-project-studio.tsx l.843-894, 910, 956, 1030, 1067-1082; retrieval-queue.tsx; the order in the reader page: Before a single paragraph of the lesson, the reader stacks three instruments (mission control, the dark 'ANGEWANDTES KURSPROJEKT · REPO' studio bar and the 'VERTEILTES ABRUFEN' queue). That is about 1,400px at 1440 and several screens at 390.
- **Repetition:** the lesson title appears three times (mission header, lesson-reference, reader), and 'Agent statt Assistent.' twice.
- **Heading order:** h2 'Welcher Beleg…' and h2 'Die Repository-Mission' come before the h1.
- **Old look in the studio:**
  - dark bar with `text-[#ffc6aa]` mono caps
  - `font-black tracking-[-0.05em]` and `tracking-[-0.08em]`
  - a 4px offset-shadow 'WERKSTATT ÖFFNEN' button
  - a dashed box
  - a pink callout with an orange left bar
  - `border-l-2 border-brand-orange` notes
- **Old look in lesson-mission-frame:** `border-l-4 border-brand-orange` and mono-uppercase chips. Fix: - **Order:** render LessonReference (the lesson text) first, then a single Kopflinie section 'Projekt zu dieser Lektion' holding mission control and the studio, with the retrieval queue collapsed below it.
- **Heading order:** make the mission prompt an h3, or move it after the h1.
- **course-project-studio.tsx:**
  - l.843: `bg-foreground text-background` becomes `border-t-2 border-foreground bg-transparent text-foreground`.
  - l.845 and l.850: the `font-mono … uppercase text-[#ffc6aa]` labels become `text-label text-muted-foreground`, and the test that pins `text-[#ffc6aa]` is updated.
  - l.868 and l.879: `font-black tracking-[-0.05em]/[-0.08em] leading-[0.98]` becomes `font-bold tracking-[-0.015em] leading-tight`.
  - l.1067: `border-2 border-dashed` becomes `border-t border-hairline pt-5`.
  - l.1082: the Mennige button with `shadow-[4px_4px_0…]` and hover translate becomes the Ink button `bg-foreground text-card font-semibold`, with no shadow and no translate.
  - l.829, l.910, l.1030 and l.1038: drop the `border-l-*` rules, use `border border-destructive p-4` plus the word 'Fehler' for errors, and plain text for notes.
- **lesson-mission-frame.tsx:**
  - l.44: becomes `text-label text-muted-foreground`.
  - l.59: drop `border-l-4 border-brand-orange pl-3`.
  - l.75 (chips): `border border-hairline px-2 py-1 text-caption text-foreground`, with no mono and no uppercase.
- [high] src/components/imported-courses/claude/claude-lesson-reader.tsx l.142-145; codex/codex-lesson-reader.tsx l.123-126; ai-native-operator/lesson-reader.tsx l.200; data-infrastructure/data-infra-lesson-reader.tsx l.131; data-science chapter hero ('— KAPITEL 03 · DATENBEREINIGUNG'): LessonReference already shows the kicker 'Lektion' and the title. Each reader then adds its own mono-uppercase orange 'LEKTION 2 VON 12' (Data Science: 'KAPITEL 03 · …' with an orange dash). The result is a doubled kicker and old-look eyebrows.
- The readers' h1 tracks at -0.03em, twice the allowed maximum.
- In the Data Science chapter, the content column starts at x=304, while the reference header starts at x=272, so there are two left edges. Fix: - Pass the progress into the LessonReference kicker ('Lektion 2 von 12', `text-label text-muted-foreground tabular-nums`).
- Delete the reader `<p className="mb-1 font-mono text-[12px] font-bold uppercase tracking-[0.12em] text-brand-orange">` lines.
- In each reader h1, change `tracking-[-0.03em]` to `tracking-[-0.015em]`.
- Remove the Data Science chapter eyebrow, and remove the inner horizontal padding of the Data Science chapter wrapper (ds-v8-scope.css `.ch-hero` / the section padding) so the text aligns with the reference title at the same x.
- [medium] src/app/kurse/open-source/claude/page.tsx l.196 and l.215; src/app/kurse/open-source/codex/page.tsx l.144 and l.165 (same pattern in data-infrastructure, DEF and operator page.tsx): The lesson ledgers use `font-mono text-xs font-bold uppercase tracking-[0.06em] text-brand-orange` for 'LEKTION 1' and 'TRACK 01, GRUNDLAGEN', which puts twelve Mennige marks in one section.
- In the 4.75rem column, 'LEKTION 10', 'LEKTION 11' and 'LEKTION 12' wrap onto two lines, and 'TRACK 02, AUFTRAGSGESTALTUNG' breaks mid-phrase.
- A vertical rule (`lg:border-r lg:border-border`) splits each track into a two-column box, which reads like a table inside the ledger. Fix: - **Lesson label:** replace the `<p>` with `<span className="text-label text-muted-foreground tabular-nums">{String(lesson.number).padStart(2,'0')}</span>`, drop the word 'Lektion', and set the grid to `grid-cols-[3rem_minmax(0,1fr)_1rem]`.
- **Track label:** 'Track 01 · Grundlagen' as `text-label text-muted-foreground`, with no uppercase, mono or orange.
- **Divider:** replace `lg:border-r lg:border-border` with nothing, and give each track a Kopflinie head (`border-t-2 border-foreground pt-4`) above its rows.
- **Arrow hover:** `group-hover:text-brand-orange` becomes `group-hover:text-foreground` plus `.arrow-nudge`.
- [medium] src/components/imported-courses/claude/claude-lesson-sidebar.tsx l.77-80; codex/codex-lesson-sidebar.tsx l.75; data-infrastructure/data-infra-lesson-sidebar.tsx l.85; ai-native-operator/lesson-sidebar.tsx l.85; ai-native/kurs/lesson-sidebar.tsx l.91; data-engineering-fundamentals/def-chapter-sidebar.tsx l.48: The course/kurs sidebar was fixed, but the seven sibling sidebars still show the active lesson with an orange left bar (`border-l-2 border-brand-orange`) and a pastel `bg-brand-orange/10` fill, which are removal items 3 and 6.
- The track group labels ('GRUNDLAGEN', 'TRACK 01, GRUNDLAGEN') are mono uppercase with 0.12em or more letter spacing.
- The label 'TRACK 04, FORTGESCHRITTEN' collides with the dev badge, which shows the text is cramped at the 240px width. Fix: - **Active item:** `bg-card-hover font-semibold text-foreground` with a leading `size-2.5 bg-foreground` square. Keep `border-l-2` out of the class list, or use `border-l-0`.
- **Inactive item:** `text-muted-foreground hover:text-foreground hover:bg-card-hover`.
- **Group labels:** `text-label text-muted-foreground mt-6 mb-2` in sentence case.
- Extract this as a shared `LESSON_SIDEBAR_ITEM_CLASS` in components/course so the seven files cannot drift again.
- [medium] src/components/course/kurs/markdown-renderer.tsx l.85 (inline code) and l.118 (links): Inline code renders as Mennige text inside a 1px box with `whitespace-normal`. In the anatomy lesson, the XML example wraps across six lines as broken, open-ended boxes, and every snippet on the page is red.
- That breaks 'one Mennige group per section', and the page reads like the old highlighter look.
- Markdown links are also Mennige. Fix: - **Inline code (l.85):** `bg-[#e5e4e2] px-1 py-px font-mono text-[0.875em] text-foreground [box-decoration-break:clone] [overflow-wrap:anywhere]`, with no border and no `text-brand-orange`.
- **Links (l.118):** use the text-link recipe `text-foreground underline decoration-hairline underline-offset-4 hover:decoration-foreground`.
- **Content (for the copy stream):** make the anatomy XML sample a fenced code block, not inline code.
- [medium] src/components/course/technical-course-landing.tsx l.96 (H1) and l.99; src/components/course/course-landing-sections.tsx l.101-105 (ledger h3 and description); src/app/eu-ai-act-kurs/page.tsx lead: Headline breaks and orphans:
- **KF H1:** `max-w-[22ch]` breaks it as 'KI im Alltag: Was / du wissen solltest.', with the question word left hanging.
- **AI-Native H1:** it runs to 4 lines at 1440 and fills the whole first screen at 390. The CTA sits at y≈500 and the facts fall below the tab bar.
- **Ledger descriptions:** single-word orphans ('generative / KI.', 'jetzt / zählen.', '(Art. / 9-43).', 'Wort / geradestehen.'), with about 500px of empty space to their right.
- **EU lead:** the date splits as '27. / Juli 2026'. Fix: - **Hero H1 (technical-course-landing.tsx):** `max-w-[26ch] text-balance`.
- **Ledger (course-landing-sections.tsx):** description `max-w-[64ch] text-pretty`, and h3 `text-balance`.
- **EU copy:** '27. Juli 2026'.
- **AI-Native H1:** shorten it, e.g. 'Mit Claude wiederkehrende Arbeit automatisieren.' (EN 'Automate recurring work with Claude.'), and update route-ai-native-locales.spec plus course-routes.locale.test.
- [medium] src/components/course/technical-course-progress.tsx (used as `progress` on the 4 native landings): A visitor who is not signed in sees '0 / 18 Lektionen … 0%' with an empty grey bar in every hero facts column. It is an empty state with no information that adds a third visual element to a calm hero. Fix: Return null until hydration shows that completed > 0, e.g. `if (!hydrated || completed === 0) return null;`. Keep the data-course-progress-card wrapper only when it renders, and adjust the kit test if it asserts the wrapper.
- [medium] src/lib/course/data.ts l.134, 226, 232 (+ KF and EU block descriptions); content/ai-native/modules.json l.26 and module 1 description; src/lib/codex/course-copy.ts l.82, 92, 95, 115 and lesson subtitles; Claude lesson content: Copy that breaks the voice rules is still visible on the landings:
- **Em dash:** 'täglich nutzt — und warum das der erste Schritt ist'.
- **Title case and ampersands:** 'Warum & Für wen', 'GPAI, Art. 4 & Transparenz', 'Governance & Sanktionen'.
- **Count-first fragments:** 'Die 4 Risikoklassen', '4-Stufen-Datenklassifikation für KI-Tools. Wissen, welche Daten…', '3-Schritt-Prüfung, …', '5-Schritte-Audit', '4 praktische Aufgaben: …'.
- **Rhetorical question:** 'Jeder Chat eine isolierte Anfrage? Muss nicht sein.'
- **Staged openers:** 'Die Werkzeuge hast du schon. Du baust daraus…' and '…für die Verwendung geradestehen'.
- **Codex:** the H2 'Vier Tracks. Zwölf Lektionen. Ein Abschlussfall.' and the kicker 'Codex / Kurs'.
- **'Not X, but Y' subtitles:** 'Agent statt Assistent', 'Diff und Protokolle sind Nachweise, keine Freigabe', 'Wähle nach Betriebsanforderungen, nicht nach Logo'; Claude 'Prompts sind Verträge, keine Beschwörungen'. Fix: - **KF 01:** 'Du findest die KI-Systeme, die du im Alltag schon nutzt.'
- **EU 01:** 'Warum es die KI-Verordnung gibt und für wen sie gilt'.
- **EU 02:** 'Risikoklassen'.
- **KF 02:** 'Welche Daten in welches KI-Tool dürfen, sortiert nach vier Vertraulichkeitsstufen.'
- **KF 04:** 'Eine KI-Antwort in drei Schritten prüfen und erfundene Angaben finden.'
- **EU 06:** 'Audit in fünf Schritten, Bezug zur DSGVO, Vorlagen und ein Fallbeispiel aus dem Mittelstand.'
- **AI-Native m02:** 'Du richtest Claude für ein festes Projekt ein, damit Anweisungen und Dateien in jedem Chat bereitstehen.'
- **AI-Native m01:** 'Du beschreibst eine Aufgabe genau, prüfst das Ergebnis und entscheidest, ob du es verwenden kannst.'
- **Codex courseTitle:** 'Lehrplan'.
- **Codex eyebrow:** 'Technischer Codex-Kurs'.
- **Codex facts:** 'Eine Übung pro Lektion'.
- **Codex subtitles:** rewrite as tasks, e.g. L01 'Wie Codex einen Auftrag im Repository ausführt', L07 'Einen Codex-PR anhand von Diff und Testlauf prüfen'.
- **Then:** run `bun run content:lint`.
- [medium] src/app/ai-native/page.tsx facts (the '12 h · eigenes Tempo' row and l.311 'kostenloses Lernkonto'): The facts column says '12 h' while the module ledger right below adds up to 50+76+89+88 = 303 min, so the page contradicts itself. The 12 h figure includes the exercises (see lib/courses/catalog-copy.ts l.87).
- Fact casing is inconsistent across the landings: 'kostenloses Lernkonto' and 'eine Übung pro Lektion' are lowercase, but 'Kostenlos, mit Lernkonto' is capitalised. Fix: - Replace the fact with 'ca. 5 Std. Lektionen, 12 Std. mit Übungen' (EN 'about 5 hrs of lessons, 12 hrs with exercises').
- Capitalise every fact: 'Kostenlos, mit Lernkonto' (matching KF and KuG) and 'Eine Übung pro Lektion'.
- [low] src/components/course/technical-course-landing.tsx l.55-56 TECHNICAL_COURSE_LEDGER_LINK_CLASS; src/app/ai-native/page.tsx l.362-368: The 'Außerdem im Kurs' rows use `px-2`, so their titles start 8px to the right of the ledger numbers and the section heads above them (x=152 vs 144). Their caption column is a fixed 14rem track that floats in mid-row and does not line up with the right-aligned lesson and minute meta of the ledger above. The claude and codex ledgers use the same class, so they are indented too. Fix: - In TECHNICAL_COURSE_LEDGER_LINK_CLASS, change `px-2` to `px-0` and keep the hover as `hover:bg-card-hover`. The tone then reaches the text edge, which is acceptable; do not use negative margins.
- On ai-native, set the grid to `sm:grid-cols-[minmax(0,1fr)_auto_1.5rem]` and the caption to `sm:text-right text-caption tabular-nums`, so it lines up with the meta column above.
- [low] src/components/widgets/tier-a/terminal-replay.tsx; src/components/widgets/claude/prompt-compare.tsx l.63 (and the Codex/Claude lesson widget shells: 'VERGLEICH', 'REPLAY', 'PRAXIS ·' blocks): The in-lesson widgets keep:
- a 3px Mennige left rule
- mono-uppercase orange headers
- offset-shadow Mennige buttons ('BEIDE SIMULIEREN', '▶ RUN REPLAY') with hover translate
- a fake terminal title bar with three dots
- an empty 'press "Run replay" to watch this session play out' state, which is removal item 16. The English string also sits on a German page. Fix: - **Widget shells:** `border border-foreground bg-card p-5`, dropping `border-l-[3px] border-brand-orange`. Headers become `text-label text-muted-foreground`.
- **Buttons:** the Ink button (`bg-foreground text-card font-semibold rounded-none`), with no `shadow-[3px_3px…]` or `hover:-translate-*`.
- **Replay:** render the finished transcript on load, with 'Erneut abspielen' as the optional replay. Remove the three-dot chrome and localise 'speed', 'Run replay' and 'Reset'.
- [low] src/components/course/course-landing-sections.tsx l.134-142 (boundary details) and l.112 (mobile ledger meta order); src/app/ai-native/page.tsx module rows: The collapsed boundary summary draws a 20px bordered '+' box, a small box that sits on its own. At 390px on /ai-native, each module's '5 Lektionen · 50 Min.' meta falls below the 'Themen im Modul +' disclosure, so the facts that describe the module come after the control that expands it. The next-course text link sits alone with about 90px of empty space above the footer. Fix: - **Boundary summary:** replace the boxed marker with a plain `+`/`−` in `text-label tabular-nums`, or a 12px chevron, with no border.
- **Mobile meta:** give the meta `order-first sm:order-none` or render it inside the title block (`mt-1 text-caption`) so it sits directly under the h3 on mobile.
- **Next-course link:** place CourseNextLink inside the last section (mt-8) and reduce the page `pb-16` to `pb-12`.

### critique:B6-site-chrome

Verdict: The chrome is mostly there. The header is a flat paper band with a hairline, the footer is a real graphit band, the tab bar is quiet, square geometry is used throughout, and axe finds no chrome violations on /workshops at 1440, 1024 or 390. Horizontal overflow is 0 everywhere. I would not call it finished yet, for three reasons. (1) The brand lockup is still the brutalist one: Arial Black at weight 900, uppercase, tracking -0.035em, it rotates -8° on scroll, and it spends Mennige twice. On top of that the scroll-progress line adds a third Mennige mark to every page. (2) Alignment is off: the footer column sits 24px inside the header and page column at 1440, and the header's two 'active' underlines sit on different baselines. (3) / fails axe color-contrast (4 nodes at 1440/1024, 6 at 390) in the unrestyled home hero and offering. That is outside B6 but it breaks the Lighthouse 1.00 contract on a page this review covers. Fixes are listed below, most severe first. Screenshots and scripts are in /tmp/claude-0/-home-user-platform/614e303c-f8f0-55ca-be18-13442a9af90b/scratchpad/impl/B6-site-chrome-critique/.

- [high] / (home), packages/website/src/components/home/offering.tsx (tiles with bg-brand-peach/50, bg-brand-sky/58, bg-brand-pink/46: dt > span.font-ui-mono.tabular-nums.text-brand-orange) and components/home/mobile-rails.tsx (a[data-home-rail-tile] > .tracking-[0.08em].uppercase.text-brand-orange; .bg-brand-peach/38 eyebrow): axe color-contrast fails on / at every width (4 nodes at 1440 and 1024, 6 at 390). The cause is orange mono-uppercase text on pastel fills. The rest of the home hero is still the risograph look the chrome now sits on top of: a cobalt/orange two-tone headline, a pink rotated blob at the left edge, '+' registration marks at x=18/1422, a boxed lead paragraph with a shadow, a rounded cobalt button, and pastel lime/peach/sky step tiles. It breaks the Lighthouse accessibility 1.00 contract and it is the first thing you see under the new header. Fix: Integrator or home owner (not B6). In offering.tsx and mobile-rails.tsx, replace the bg-brand-(peach|sky|pink|acid)/* tile fills with bg-background plus border-t border-hairline rows. Change the eyebrows 'font-ui-mono uppercase tracking-[0.08em] text-brand-orange' to 'text-label text-muted-foreground' and the numerals to 'font-ui-mono tabular-nums text-muted-foreground'. In components/home/hero.tsx, make the headline a single colour (text-foreground, no text-brand-cobalt / text-brand-orange spans), remove the decorative blob and cross marks, change the lead box to a plain p.text-lead.max-w-[56ch] (no border, no shadow, no rounded) and the CTA to the square Mennige primary button. Then rerun axe on /.
- [high] packages/website/src/components/nav.tsx LogoWordmark (l.148 iconRotate, l.161 m.div[data-logo-mark], l.181 span[data-logo-wordmark] style, l.203 span.text-brand-orange) + tests/e2e/site-header-logo-mark.spec.ts: The lockup is the loudest leftover of the old look. The wordmark is 'Arial Black' at weight 900, uppercase, letterSpacing -0.035em, which breaks three rules at once: no font-black, no uppercase, tracking no tighter than -0.015em. The Mennige square rotates -8° on scroll; at the bottom of the phone page it sits tilted in the 48px bar (m-end.png), which is exactly the 'rotated' gimmick the owner asked to drop. The footer sets the brand as a lowercase 'loehrning.ai' in Loehrning Sans 700, so the site now shows two different logos. Fix: Change the wordmark style to {fontFamily: 'inherit', fontWeight: 700, letterSpacing: '-0.015em'} and drop the 'uppercase' class. Render 'l' + 'oehrning' + '.ai' so it matches the footer (keep the leading-L collapse spans if you want the merge). Change iconRotate to useTransform(scrollY,[0,160],[0,0]) or remove the rotate style entirely. Update site-header-logo-mark.spec.ts l.106 from 'settled.squareTransform not none' to 'toBe("none")'. That is a legitimate old-look pin; the L-merge and geometry assertions stay. If the owner treats the lockup as fixed brand, raise it as an explicit decision instead of letting it through silently.
- [medium] packages/website/src/components/footer.tsx l.198 container div: At 1440 the footer column starts at x=168 and ends at 1272. The header wordmark and every page column (/workshops CONTAINER mx-auto max-w-[75rem] px-6) start at 144 and end at 1296. The footer's left edge is 24px inside everything above it, which shows as soon as you scroll from the last section into the band. Fix: Change 'mx-auto w-full max-w-6xl px-4 py-10 sm:px-6 sm:py-12' to 'mx-auto w-full max-w-[75rem] px-4 py-10 sm:px-6 sm:py-12', the same container token the learning pages use. The header's lg:px-[max(1.5rem,calc(50%_-_36rem))] already resolves to 144.
- [medium] packages/website/src/components/ui/scroll-progress.tsx l.15 and l.18 (mounted in app/layout.tsx l.142): Every page gets a 1px Mennige/25 track and a 2px Mennige fill across the top edge, above the new flat header (visible in every screenshot). Together with the Mennige logo square and the Mennige '.AI', the persistent chrome spends three Mennige marks. The rule is one Mennige group per section. It also reads as a progress bar with no job on short pages like /workshops. Fix: Integrator: remove <ScrollProgress /> from layout.tsx. If the owner wants to keep it, change 'bg-brand-orange/25' to 'bg-transparent' and 'bg-brand-orange' to 'bg-foreground/60', so it is ink and not a second accent.
- [medium] packages/website/src/components/nav.tsx l.203 <span className="text-brand-orange">.AI</span>; footer.tsx l.211 <span className="text-brand-orange">.ai</span>: The header spends Mennige twice, on the square and on '.AI', one right after the other. On graphit the footer puts '.ai' in #e07050 as decoration. The Mennige budget is for the one thing to look at, and neither of these is it. Fix: In nav.tsx change '.AI' span to 'text-muted-foreground' (Schiefer 8.08:1), keeping the Mennige square as the single brand mark. In footer.tsx change the '.ai' span to 'text-muted-foreground' (Leinen on graphit 11.79:1).
- [medium] packages/website/src/components/i18n/language-switch.tsx l.73 underline span; nav.tsx l.456 and l.591 (border-y-2 on triggers/links): The two 'current' markers sit on different baselines. The nav group and link underline is the button's 2px border-bottom at y≈52–54. The DE underline is an absolute bar at 'bottom-2' (y≈44–46) and is a fixed 20px wide, while the nav underline runs the width of the text. Side by side in the bar (Praxis ... DE), it looks like two different components. Fix: In language-switch.tsx change the span class 'absolute bottom-2 left-1/2 h-0.5 w-5 -translate-x-1/2' to 'absolute bottom-0 inset-x-2 h-0.5'. The underline then sits on the same y as the nav border-b and spans the label like the others.
- [medium] packages/website/src/components/nav.tsx l.568 js-desktop-nav wrapper and l.609 before <LanguageSwitch />: The utility cluster (DE, EN, GitHub icon, Login) follows the content links (Lernen ... Über mich) with the same 16px gap. At a glance 'DE EN' reads as two more nav items, and the right side of the bar has no grouping. Fix: Put a separator before <LanguageSwitch /> in the desktop row: <span aria-hidden="true" className="mx-2 h-5 w-px bg-hairline xl:mx-3" />. Or give LanguageSwitch className="ml-4 xl:ml-6" there. Either separates navigation from settings without adding a box.
- [medium] packages/website/src/components/nav.tsx l.713 mobile dialog sheet (className 'mx-2 mt-2 overscroll-contain border border-foreground bg-card shadow-overlay sm:mx-3') and l.719 dialog header row with a second <LanguageSwitch />: On the phone the menu is a floating card with an 8px gutter of dark hero showing on each side (m-menu.png), so it reads as a box over a box. It also shows two DE/EN switches at once: one in the still-visible header bar and one in the sheet's top row next to the X. The sheet has no title, so the top row is just an X, empty space and a duplicate control. Fix: Change the sheet to 'mx-0 mt-0 border-x-0 border-t-0 border-b border-foreground bg-background shadow-overlay' so it hangs flush under the bar as one flat sheet. Remove the <LanguageSwitch /> from the dialog header row, since the bar's switch stays visible. Put a text-label 'Menü' / 'Menu' at the left of that row and the close button at the right.
- [medium] packages/website/src/components/auth/auth-status.tsx l.83 mobile variant: In the phone menu the Login button is full width with 'justify-between'. The icon sits at the far left and 'Login' is pushed to the far right edge (m-menu.png), which reads as a layout bug. Fix: Change mobile && "mt-3 w-full justify-between px-4" to mobile && "mt-3 w-full justify-start gap-3 px-4". Or use justify-center and keep the icon next to the label.
- [low] packages/website/src/components/footer.tsx (link group data feeding l.264-290) vs nav.tsx lernenNavItems / practice items: The footer IA does not match the header. The header group is 'Lernen' (Alle Kurse, Grundlagenpfad, Technikkurse, KI-Check, Lernbücher) and Open Source is top level. The footer calls the group 'Kurse', drops KI-Check and files Open Source under 'Praxis'. Two footer groups repeat their own heading as their only or first link ('Blog' → Blog, 'Über mich' → Über mich), which leaves a one-row column at 1440 and 1024. Fix: Use three footer groups that mirror the header. Lernen: Alle Kurse, Grundlagenpfad, Technikkurse, KI-Check, Lernbücher. Praxis: Workshops, Praxisbeispiele, Blog, Open Source. Über mich: Über mich, Hilfe, Rückmeldung. Change the grid 'md:grid-cols-4' to 'md:grid-cols-3' (l.264). This removes the one-link Blog column and keeps the labels consistent in DE and EN.
- [low] packages/website/src/components/footer.tsx l.199-201 (brand column vs link columns): Each link column starts with a hairline at y≈48 and its heading at y≈70. The brand column has no line and its 'Freie Lernplattform' label starts at y≈57. The top of the footer is therefore ragged: four ruled columns and one unruled one, on different baselines. Fix: Add 'border-t border-hairline pt-3' to the brand column div (l.200 'min-w-0' → 'min-w-0 border-t border-hairline pt-3'). All five columns then share one top line and one label baseline, which is the Kopflinie-and-hairline logic used everywhere else.
- [low] packages/website/src/components/footer.tsx l.199 top block 'border-b border-hairline pb-8': At 390 the closed 'Alle Bereiche' disclosure has about 20px above its label and about 55px of empty graphit below it before the next hairline (m-end.png). The pb-8 was sized for the desktop columns, so on the phone the band looks unfinished. Fix: Change 'pb-8' to 'pb-2 lg:pb-8' on that grid (the summary row already brings min-h-11 plus py-2).
- [low] packages/website/src/components/nav.tsx l.483 dropdown 'absolute left-0 top-full mt-2 w-64 ...': The trigger is 44px tall, vertically centred in the 64px bar. With top-full mt-2 the sheet's top edge lands at y≈62, two pixels above the bar hairline at 64. The ink border and the hairline double up into a smudge (d-dropdown.png) instead of the sheet hanging cleanly from the bar. Fix: Change 'mt-2' to 'mt-[calc((var(--nav-h)-2.75rem)/2)]' (10px) so the sheet's top border sits exactly on the bar's bottom hairline. Alternatively, make the trigger wrapper 'h-full' and use 'top-full mt-0'.
- [low] packages/website/src/app/globals.css l.40-47 @font-face 'Loehrning Sans Fallback' (src: local("Arial")): With font-display: optional, a first visit without a cached font renders the fallback. On Linux there is no Arial, so the fallback face errors (document.fonts shows status 'error' on /workshops) and text drops to DejaVu Sans. DejaVu is much wider: the desktop nav shifts left by 28px ('Lernen' is 69px wide on / and 73px on /workshops), and the whole /workshops hero and header render in DejaVu in the fresh screenshots. It is not a B6 file, but it makes the chrome look inconsistent from page to page. Fix: Integrator: change the src to 'local("Arial"), local("ArialMT"), local("Liberation Sans"), local("Helvetica")' so metric-compatible faces are used, and add <link rel="preload" as="font" type="font/woff2" crossorigin href="/fonts/loehrning-sans-regular-v1.woff2"> (and bold) in app/layout.tsx if the font-loading contract allows it.
- [low] packages/website/src/components/footer.tsx l.316-327 data line ('Datenstand: Q3 2026  Aktualisiert: 2026-09-05'): The line mixes a Loehrning Sans label with Geist Mono values, and 'Q3 2026' is a period, not a machine value. It is set wider than the surrounding caption, so the bottom-right corner reads as debug output. It also shows two dates for what the reader sees as one fact. Fix: Keep mono only on the ISO date: remove 'font-ui-mono' from the STAND_DATE span (l.321) and keep 'tabular-nums'. Consider dropping 'Datenstand: Q3 2026' and showing only 'Aktualisiert am 5. Sept. 2026' (voice: provenance stated plainly), if the font-loading contract only requires that the shell never uses font-mono.

### critique:deck

Verdict: The deck is close to W03 quality, but it isn't ready to present yet. I walked all 102 states at 1440x900 with reduced motion, opened the 9 appendix pages and drove the presenter console. Results: 0 console errors, 0 page errors, 0 failed requests and 0 CSP violations. The only overlap flagged was the-arc wire SVG, which is there by design. No en or em dashes turned up in slides.html, presenter-notes.js, the scene CSS or w04-fill.js, and the slop-word grep found nothing. I checked every on-screen number in the final state of each scene against w04-data.json. The only numbers not in the JSON are "76 minutes" on the cover and the regulatory reference numbers (Directives 2026/470, 1560, 1563, 825; UBA 433; 76.9%), which are allowlisted, so every case number traces to the JSON. The problems are about teaching and design. (1) Two votes are spoiled before they are cast. On anatomy, the title "48.7 tonnes apart" and the labelled bars answer the prediction. On what-drove-it, the struck-through efficiency sentence and the "Grid factor" axis label give away the vote. (2) The two bridges on what-drove-it use different, broken scales: 744 t is drawn only 1.4 times as tall as 72.2 t. (3) The labels the brief requires are missing. The deck never says "constructed from documented failure modes" (0 hits in slides.html) or "illustrative" (0 hits). (4) The text is denser and smaller than in W03: I found 16 to 21 px text in the ledger, the factor table, the trace cards, the month-grid tags and the bill cards. (5) Mennige marks two or three things per step on several scenes. W03 is calmer: bigger type, fewer words, one Mennige mark per step. Screenshots, including W03 comparison shots, are in /tmp/claude-0/-home-user-platform/614e303c-f8f0-55ca-be18-13442a9af90b/scratchpad/impl/w04/deck-critique/ (shots/, shots/presenter/, w03/). The scripts are walk.mjs, fonts.mjs, nums.mjs, presenter.mjs and w03.mjs.

- [high] anatomy, steps 0 and 1 (prediction vote "How far is the AI's total from the right one?"), slides.html section#anatomy: The vote is answered before the room votes. The title reads "Six errors, 48.7 tonnes apart", and both end bars carry value labels (1,866.5 and 1,915.2). The broken axis also makes the gap look like about 10%, which nudges people toward the wrong option. Fix: Until press 5, set the title to "What do six errors do to the total?". Put "48.7 t apart: 2.5%" ({gap_lb_t}, {gap_lb_pct}) on the swing-band caption at press 5, or swap the title text at step >= 2 through data-step-current. Hide the 1,915.2 value label on the Right total bar until press 2. Log this as a deliberate change from the SPEC §2.3 title.
- [high] what-drove-it, steps 0 and 1 (room vote "What made location-based emissions fall 102.3 t?"): The vote is spoiled. The AI's sentence "mainly from efficiency measures" is already struck through, and the empty bridge's first axis label reads "Grid factor". Both give away the answer, and the scene exists to catch the room picking 'efficiency'. Fix: At steps 0 and 1, show the AI sentence without the strike. Draw the strike at press 2, when the location-based bars appear. Hide the driver axis labels (dv-name for the middle columns) until their bars draw, and keep only the 2024 and 2025 labels visible during the vote.
- [high] what-drove-it, the two bridges side by side: The panels use different scales, and both axes are broken. The 2,017.5 and 2,674.5 bars are drawn the same height. The -744.0 certificate bar is only about 1.4 times the height of the -72.2 grid bar, although the value is 10 times larger. Side by side, readers will compare the heights and draw the wrong conclusion. Fix: Draw both bridges on one shared y-scale with the same axis break, for example from 1,800 t up to 2,700 t, so -744.0 reads as ten times -72.2. If a shared scale leaves the LB bars too thin, stack the two bridges vertically and add "Different scales" under each panel heading.
- [high] raw-answer label, anatomy caption, resolution raw card (meta.constructedLabel_en): The brief says the raw-folder answer must always be labelled 'constructed from documented failure modes'. The phrase appears only in the presenter notes; slides.html has 0 hits. The label is also the smallest, greyest line on the answer card (resolution: 20 px slate), and on raw-answer and resolution it wraps with an orphaned "run.". Fix: Change the on-screen label to "Constructed from documented failure modes: what the answer looks like when all six traps fire. Not a recorded run." Change it in meta.constructedLabel_en in build_dataset.py and rebuild, so the JSON stays the single source. Set .rs-ans__label and the raw-answer label to at least 24 px in ink, and allow two lines without an orphan (text-wrap: balance).
- [high] Every scene with the case strip "Fictional company · teaching factors" (23 places), six-rules factor note, cover meta line, limits row 5: The brief says factors are labelled as illustrative teaching values. "illustrative" appears 0 times in the deck, and the strip says "teaching factors", which a controller may read as a real method. Fix: Change the strip to "Fictional company · illustrative teaching factors". Change the note under the factor table to "Illustrative teaching values, not official factors." (update meta.factorLabel_en). Change the cover meta line to "... · illustrative teaching factors".
- [medium] the-ledger table (th 16 px, td 18 px mono), six-rules and appendix-factors factor table (18 px; unit column 16 px), trace-two file names (.tt-files 16 px), month-grid tags (.mg-tag 16.8 px), whose-bill E-TB-01 18.9 px, rematch row IDs 20 px, bill and certificate cards (.doc 20 to 21 px), anatomy and what-drove-it axis names 20 to 20.8 px: This text is under 22 px on the 1920 canvas and can't be read from the back of the room. The ledger and the factor table are the teaching objects of act 3, so they must be readable. Fix: Keep all text at 22 px or more. Ledger: show 6 columns instead of 8 (drop unit_source and period, or fold period into row_id), set td to 22 px and th to 20 px sentence case with 24 px row height, or show 3 rows at a larger size. Factor table: drop the year column (the ID already carries it), set the unit to 20 px and values to 22 px. trace-two: file names at 20 px, or show only the short name. SVG axis labels (wf-name, dv-name, mg-month) at 24 px. Doc cards: at least 22 px; cut less important lines such as the Postfach and "(erfundener Lieferant)" on slides.
- [medium] month-grid, steps 3 to 6: labels "annual statement", "Q1" to "Q4" and "one bill" drawn on top of the hatch: The diagonal hatch runs through the letters, so the labels are hard to read, and they are only 16.8 px. Fix: Give .mg-tag a paper knock-out (an SVG rect in var(--paper) with 6 px padding behind the text, or paint-order stroke in paper at 6 px), and set it to 20 to 22 px bold.
- [medium] month-grid legend: "Bill in the folder" and "One bill, several months" both use a hatched swatch and can't be told apart. The Talbrück bar adds a third, dashed-hatch style that the legend doesn't explain. Fix: Draw "One bill, several months" as a hatched swatch with a solid ink bracket over two cells, matching how the Nov/Dec bill is drawn. Add a legend row "Another company's bill" with the dashed-outline hatch used for Talbrück.
- [medium] whose-bill step 3; month-grid step 6; one-unit step 4; six-rules step 4: Several elements are in Mennige at once. whose-bill has the addressee box, the answer box and the +400.0 t box. month-grid has the "2" badge, the OCR box and the net-line rail. The rule is one figure in Mennige per step, and W03 keeps to one. Fix: Keep Mennige only on the element introduced by the current press. When the next press comes, earlier accents drop to a 2 px ink outline, using the data-step-current pattern already used on what-drove-it and resolution. On whose-bill step 3, only "+400.0 t location-based" stays Mennige.
- [medium] six-rules rule 2 "Grid first. One key: invoice + period + meter."; rule 6 "No claim words without numbers.": In an electricity deck, "Grid" reads as the power grid. Rule 6 is too compressed for a non-technical controller. Fix: Rule 2: "Fill the site-by-month table first. Count each bill once: invoice number, period and meter." Rule 6: "Every number names its rows. Words like 'green' or 'efficient' need a number behind them." Mirror both in grenzen_und_regeln.md.
- [medium] month-grid step 6 net line "+205,000 − 200,000 = +5,000 kWh": The terms have no labels. The room has to guess that 205,000 is the duplicate March bill and 200,000 is the missing October. Fix: "Duplicate March +205,000 kWh − missing October 200,000 kWh = +5,000 kWh off" (same data-num keys), then one short line: "Two errors, almost no trace in the total."
- [medium] two-scope-2 steps 0 to 6: stacked bar (WN + LO hatched) and bridge (residual-mix bar hatched): This clashes with the deck's own encoding (SPEC §2.1: hatch = raw input, solid ink = approved figure). Here the hatch means "no certificate" and "residual mix", which are ledger figures. Fix: Draw WN + LO 2,370 MWh as an ink-outlined empty bar and the Werk Süd certificate part as solid ink with the certificate icon. Draw the residual-mix bridge bar in solid ink and the certificate bar as an outline with a minus sign. Keep the hatch only for raw-folder material.
- [medium] the-ledger steps 1 to 7 column headers (row_id, entity_on_document, qty_source, qty_norm ...): Snake_case CSV headers are data-engineer jargon. A German Mittelstand controller has to decode them while the room is completing rows. Fix: Keep mono for the values. Make the headers two-line: a plain sentence-case label ("Row", "Company on the bill", "Period", "As printed", "Unit", "kWh", "Status", "Source: file and quote") with the CSV field name below it in 18 px slate mono, or put the CSV names in the kit only.
- [low] lib/deck-runtime.js line 1199 (presenter console status "Recorded deck · no live checks") and presenter-notes.js rematch note "the recorded runs are in appendix A3": In this workshop, "recorded" means recorded AI runs, and none exist yet. The console banner and the rematch note both suggest recorded material, which contradicts M7 and M20. Fix: Change the fallback status to "Static deck · no live checks". Reword the rematch note (scripts/workshop04/notes/20-act4-6.mjs, then regenerate) to: "This is the answer we expect. Recorded runs will go into appendix A3 once they are captured; the capture protocol is there now."
- [low] raw-answer answer text "Scope 1 516.0 t CO₂e" (also resolution and the rematch "Scope 1 2025: 471.2 t"): When read aloud or skimmed, "Scope 1 516.0" runs together as "1,516.0". This happens in the most important sentence of act 1. Fix: Add a colon: "Scope 1: 516.0 t CO₂e, Scope 2: 1,350.5 t CO₂e (location-based)". Change it in the answer template and in the kit's constructed-answer text so they match.
- [low] trace-two card 3 and the timer stamp: The instruction line says "rows, file, quote, factor, arithmetic", but card 3 has only three fields. At step 2 the row list breaks inside an ID ("E-LO-" / "Q1"). The 4-minute timer uses the calendar icon, as do rule 2 and limits row 4. Fix: Add a "File and quote" blank to card 3, or change the instruction to "rows, factor, arithmetic, and file and quote where there is one". Set white-space: nowrap on each row ID span. Use the W03 stopwatch pictogram (the one on the W03 semantic-contract footer) for the timer.
- [low] ask-back steps 2 to 4: Once the tray empties it keeps its height, which leaves a gap of about 130 px between the title and the columns. Card 3's heading also changes from the tray wording ("Write that we run on green electricity.") to "We run on green electricity.", so the room loses track of which card moved. Fix: Collapse the tray when it is empty (grid-template-rows transition to 0), or move the columns up and let the tray sit underneath. Keep each card's heading identical to its tray text.
- [low] two-scope-2 HKN card and raw-folder bill cards (documents[].lines rendered with hard line breaks): Source lines break mid-phrase at card width ("für die Liefermenge an" / "Kellbrunn ..."), which makes the cards look ragged and cramped next to W03's clean nodes. Fix: For slides, join continuation lines into one paragraph per logical field and apply text-wrap: pretty. Widen the HKN card to 560 px, or cut the "Erzeugungsjahr" line on the slide (the kit keeps the full text).
- [low] six-rules and appendix-factors factor table, "basis" and "year" cells: The table shows the German "alle" in the English deck, and the basis column is empty for electricity rows, which looks like missing data. Fix: Render year "all" (EN) with the CSV value kept in the kit, and write "n/a" or "kWh" in the empty basis cells, or drop the basis column and show Hs/Hi only in the gas IDs.
- [low] cover meta line and kicker; appendix-regulation item 6: "76 minutes" is not a JSON value (the main path is 76.5 min) and reads oddly for a 90-minute slot. The kicker shows "Rules as of 26 September 2026" where SPEC and W03 put the session date. In item 6, "§ 5" and "UWG" break across lines. Fix: Change the meta line to "90 minutes · one fictional company · illustrative teaching factors", as W03 shows the slot. Put the session date in the kicker ("Workshop 04 · <session date>") and move "Rules as of 26 Sep 2026" to the footer. Use "§&nbsp;5&nbsp;UWG" in the regulation text and the sources.

### critique:demo

Verdict: Not ready to ship yet. I checked every figure on the page against w04-data.json and SPEC.md (lanes, gap line, waterfalls, all guided steps, raw and market-based meters, drivers, ranking shares, control total, bill arithmetic) and none is wrong. The page gave 0 console errors and 0 CSP violations at 1440 and 390, with no horizontal overflow and no en or em dashes. The \"constructed\" and \"teaching values\" labels are present where the spec asks for them.

Two things block shipping. First, the central chart's default state shows Fix and Running total numbers that do not chain from its own start row (1,915.2 followed by 1,784.5). Second, both gauges collapse to 0x0 because `.gauge` is an inline span, which leaves a stray tick over the \"Right answer\" text. Two more hurt the teaching: the market-based sign and order difference (+123.0 in the row, −82.0 in the bar) has no caption, and the duplicate-bill drawer's tables overflow their cards.

Smaller issues: the caption says \"down to the right answer\" when the raw answer is below it, the solid and hatched bars have no legend, the axis cuts in the bridges exaggerate the fall, and the coverage caption contradicts the grid after the raw preset. The guided steps are missing their \"why it matters\" lines, and two inline scripts go against the keep-scripts-external rule. The rest is polish: copy, the German codes and field values, 12 to 13 px labels, and matching the q-card to W03.

Screenshots and scripts are in /tmp/claude-0/-home-user-platform/614e303c-f8f0-55ca-be18-13442a9af90b/scratchpad/impl/w04/demo-critique/ (d-* for desktop, m-* for mobile, shot.mjs, g.mjs).

- [high] demo.html section 2 board, default load state (mask 0, LB): Fix and Running total columns: All switches read "Fixed" and the top row says "This answer: every trap fixed 1,915.2", yet the Fix column shows −82.0 / +80.0 / −400.0 / +495.5 and the running totals 1,784.5 / 1,864.5 / 1,464.5 / 1,960.0 / 1,918.2. Those are the ghost raw path, which starts at 1,866.5. 1,915.2 − 82.0 is not 1,784.5, so the first thing a controller sees is a column that does not add up. The "Sum of the bars = chart total ✓" line next to it makes it worse. Screenshot: demo-critique/d-sec-traps.png. Fix: In the default state, leave the Fix and Running total cells empty (or show "·") and keep the ghost path only as dashed outline bars. Alternatively, when no trap is active, make the start row the ghost start ("Raw-folder answer, 1,866.5", dashed) so the ghost numbers chain from it. Set the ghost-number cells to slate italic with a column caption "path from the raw-folder answer". The check line should only show when the drawn bars are the live state.
- [high] section 2 meters: .gauge / .gauge--vs (CSS line ~146, markup ids g-dist, g-vs, g-vs-right): .gauge is an inline <span> with no display rule, so both gauges render at 0x0 (measured in the browser: 'inline 0x0' twice). The distance bar never shows. The change-vs-2024 marker collapses into a stray 1 px tick over the "R" of "Right answer: −5.1%", visible at 1440 and 390 (d-10-raw-view.png, m-11-raw-traps.png). Fix: Add `.gauge{display:block}` (plus `width:100%`) so the 8 px track, the Mennige distance fill and the ink/dashed markers render. Then check that the dashed right-answer marker sits inside the track and no longer touches the sub-label.
- [high] section 2, Market-based, raw preset (d-12-raw-mb.png) and the board caption: Row T1 reads "+123.0 t if only this trap fires" while its Fix cell reads −82.0 (T2 −120.0 vs +80.0, T3 +600.0 vs −400.0). SPEC 3.1 requires a caption explaining that both are right because T7 is still active in the bar and not in the row. The page has no such caption, so a controller will read it as an error. Fix: When method = MB and T7 is active, add a caption line under the chart: "In market-based, T1 to T3 are fixed while T7 still prices power at 0.40, so their bars (−82.0, +80.0, −400.0) differ from the rows (+123.0, −120.0, +600.0), which use the residual mix 0.60. Both are right." Build the numbers from traps[i].isolated.mb_en and waterfall.mb. Also rename the column header "Fix" to "Fixing it changes the total by" so the opposite signs read as intended.
- [high] evidence drawer, duplicate view (Scan_Rechnung_Maerz.md / 2025-03_Strom_WN.md side by side), desktop 1440: The bill tables overflow the paper cards. The Betrag column is cut ("44.075,0", "52.502,8"), and the right card's content runs past the drawer edge at x=1440 (d-30-drawer-Scan_Rechnung_Maerz_md.png). This is the headline trap's evidence. Fix: In the side-by-side view, give each .paper `min-width:0; overflow-x:auto` and let the table use `table-layout:fixed; width:100%` with `font-size:13px` and `white-space:normal` for Position, or stack the two cards vertically when the drawer is narrower than ~900 px. Also widen the drawer to min(900px, 70vw) for this view.
- [medium] section 2 board caption strings (demo-app.js; 3 occurrences of "down to the right answer"): The caption reads "Each bar fixes one trap, from the raw-folder answer down to the right answer". The raw answer (1,866.5) is below the right answer (1,915.2), so fixing goes up. The only-T3 caption, "from this answer down to the right answer", is wrong for every below-state too. This is an accuracy slip on the core chart. Fix: Replace "down to the right answer" with "to the right answer" in all three strings. For example: "Each bar fixes one trap, in this order, from the raw-folder answer to the right answer."
- [medium] section 2 waterfall bars (fix bars) and the switch buttons: Fix bars that lower the total are solid ink, and bars that raise it are hatched, with no legend. The hatch is already the page's sign for "raw folder" (lane header, start bar), "As the AI did it" (switch) and "outside the boundary" (grid), so one texture carries four meanings. The "As the AI did it" label also sits directly on diagonal hatching, which hurts legibility (d-11-raw-traps.png). Fix: Draw fix bars in one encoding: ink fill for "fixing lowers the total", ink outline for "fixing raises it", with a two-item legend above the chart column. Keep the hatch only for the raw start bar and the grid's boundary cells. For the switch's active state, put the hatch only on the knob track (left 36 px) and set the text on a solid paper chip, or use an ink fill with paper text.
- [medium] section 4 bridges (LB and MB), start and end bars: The 2024 and 2025 total bars use a cut axis. The LB 2025 bar is about 42 px against 64 px for 2024 (a 5.1% fall looks like about 35%), and the MB 2025 bar is about 38 px against 206 px. The "//" marks are small and nothing says where the axis starts. For a controller this overstates the fall. Fix: Either draw the totals as full-length bars on a zero-based scale (the driver bars stay readable at 1440, since −72.2 of 2,017.5 is about 16 px on a 450 px track), or keep the cut and print a caption "Axis starts at 1,800 t (LB) / 1,350 t (MB); totals are not to scale". Take those start values from a computed constant, not typed. Apply the same to the board's start and end bars.
- [medium] section 5 coverage caption (#cov-caption) after the raw preset or any T1/T2/T3 switch: With the raw preset active and the toggle unpressed, the grid shows March "2", October "0" and the Talbrück row. The caption still says "As the ledger has it. October comes from the meter readings (grade B)." (d-20-cov-raw.png), so the text contradicts the grid. Fix: Build the caption from the state. When the toggle is off and any of T1, T2 or T3 is active, write "As the ledger has it, with the traps you switched on in section 2: March counted twice / October empty / Talbrück bill included." Only print "October comes from the meter readings" when T2 is fixed.
- [medium] header lede, h1, and every constructed label ("all six traps fire") vs seven switches T1 to T7: The page says "six of its parts are wrong" and "all six traps fire", but section 2 shows seven switches, and in market-based all seven are active in the raw preset (T7 −474.0). A controller counting switches will not find six. The label is also attached to the MB total 1,866.0, where seven fire. Fix: Keep meta.constructedLabel_en verbatim (it is the spec label), and add one sentence to the section 2 intro: "Six traps change the location-based total. The seventh, T7, only touches the market-based figure." Also give T7's row name a visible tag "market-based only" instead of the parenthesis in the title.
- [medium] section 2 guided sequence, steps 2 to 5 (demoSequence rendering): The steps give actions and numbers but drop the teaching point SPEC 3.1 attaches to each. Step 2 lacks "A rise of 14.8% makes anyone ask what happened, so this state would be caught", and step 3 lacks "Plausible again." Step 4 has no "so what". "Switch on only the Talbrück bill" uses words that match no control, since the switch labels are "Fixed" / "As the AI did it". Step 5's result line repeats the +2.0 t / +3.0 t already in the action text. Fix: Add a slate "Why it matters" line per step from the spec text (the numbers still come from JSON keys). Step 2: "+14.8% against 2024 makes anyone ask what happened, so this error gets caught." Step 3: "−9.8%: plausible again, so nobody asks." Step 4: "Market-based hides the unit error completely; −6.8% looks fine." Rephrase actions as "Set only the Talbrück bill to 'As the AI did it'." Replace step 5's result line with the live total "1,917.2 t · −5.0% vs 2024" from combinations[3].
- [medium] demo.html script blocks (lines ~770 and ~939 inline executable <script>): The page runs two inline executable scripts (demo-core and demo-app). The build brief says to keep scripts external like W03, which has no inline scripts in any page. CSP allows 'unsafe-inline' today, but a later CSP tightening would break the demo silently. Fix: Have scripts/workshop04/build-demo.mjs write lib/w04-demo-core.js and lib/w04-demo.js and reference them with <script src defer>. Keep the data as <script type="application/json" id="demo-data"> (not executed) or move it to lib/w04-demo-data.js. Add a build assertion that there is no inline <script> without type="application/json".
- [medium] evidence drawer, 'Check that catches it' lines (3 drawers) and the duplicate intro: The copy is clunky for a non-technical controller. "Check that catches it: Unique key: invoice no. + period + meter (fallback: ...)" stacks two colons and '+' shorthand. The duplicate drawer then says 4711-03 twice ("Both files carry invoice 4711-03 ... Same invoice number 4711-03; OCR noise in supplier name and a stray line break, invoice number clean."). The Talbrück drawer uses the raw column name "entity_on_document column + written boundary". Fix: Render as a labelled line: "Caught by" followed by plain text. Duplicate: "Caught by the unique key: invoice number, period and meter. Both files carry invoice 4711-03 for March and meter …WN01. The scan has OCR noise in the supplier name ('Enerqie'), but the invoice number is clean." Talbrück: "Caught by the 'entity on document' column and the written boundary. Contract party, customer number and meter all belong to Talbrück; Kellbrunn's purchasing only checks the invoice."
- [low] evidence drawer ledger-row card (all drawers): The field values mix languages and formats inside an English card: "In boundary: ja/nein", "Proposed by: Reader (KI-Vorschlag)", "t CO₂e (as in the CSV): LB 333,0 · MB 333,0" directly above an arithmetic line reading "333.0 t". "Normalised: 1850000 kWh(Hs)" and "200000 kWh" have no thousands separators, and the October row's Source reads "..., page Zeilen 30.09.2025 und 31.10.2025". Fix: Keep the raw CSV values but put an English display value first: "In boundary: yes (CSV: ja)", "Normalised: 1,850,000 kWh (Hs basis)", "t CO₂e: 333.0 LB · 333.0 MB (CSV: 333,0)". For csv sources, drop the "page" prefix ("rows 30.09.2025 and 31.10.2025"). Format every number with the core's EN formatter.
- [low] section 2 guided step 1 feedback (#p-status in demo-app.js) and trap T4 name: The feedback nests quotes ("Now switch “"1.240 MWh" read as 1,240 kWh” to ..."), which renders “"1.240 MWh"...”. Straight and curly quotes are mixed across the page (T4 title, step 1, step 6 "72.2 t"). Fix: In the p-status string, drop the outer curly quotes around TRAP.T4.en ("Now set T4 to “As the AI did it” and read the total."). Normalise the display strings to curly quotes “ ” in the build (only for rendering; leave the JSON untouched), or quote with the trap ID instead.
- [low] section 2 trap name column at 1440 (T4, T5 rows): The long filenames break the row layout. For T4, "Jahresuebersicht_2025_Oekostrom.md" drops to its own line, outdented from the tag. For T5, "Gas_Jahresrechnung_WN_2025.md (and WS)" wraps to "WS)" on a second line, and the title ""1.240 MWh" read as 1,240 kWh" wraps to a lone "kWh". It looks ragged next to W03's clean rows. Fix: Widen the name column (grid-template-columns: minmax(300px,1.2fr) …) or shorten the visible link text to the document type ("Annual statement, Werk Süd", "Gas bills WN + WS") and put the filename in the title attribute and the drawer header. Keep the tag and link on one flex row with `flex-wrap:nowrap; min-width:0` and ellipsis.
- [low] section 5 coverage grid cell codes and legend: The codes J (annual), Z (meter reading) and S (split bill) are German initials (Jahres-, Zählerstand) on an English page, so an English reader has to learn them from the legend. The August note sits directly under the legend with no spacing (d-sec-coverage.png). Fix: Use English codes: Y (annual bill), M (meter reading, grade B), ½ or 2m (part of a two-month bill), Q (quarterly), keeping the legend text. Add margin-top:16px before the August note and set it as a slate flag line ("August 120,000 kWh: real dip, plant holiday. Flag it, do not correct it.").
- [low] section 2 two-month row (non-interactive): The copy repeats itself and hedges: "0 t in the sum; keeps the file count at 12 and hides the missing October. No switch: it changes nothing in the sum." Fix: Show trapNotes.twoMonthBill_en as is, then "No switch, because it adds nothing to the total." Or drop the second sentence and show a greyed "no switch" chip in the switch column.
- [low] text sizes: .iso__tail, .linkish, .tag, .meter .label/.sub, grid legend, mobile .lane dt (12 to 13 px): Much of the working text is 12 to 13 px slate: "if only this trap fires", "Only this trap", the role tags, the meter labels, the coverage legend, and on phones the lane labels (12 px) in two squeezed columns. Scaled to W03's 1920 canvas that is about 16 to 17 px, well under the 22 px deck floor, and it is the text that explains the controls. Fix: Set 14 px as the demo minimum (15 px for .iso__tail and meter labels) and keep 12 px only for legend glyphs inside cells. On phones, stack the lanes (raw first, as SPEC 3.3 asks) instead of two 170 px columns, so the dt labels can be 14 px and the values 20 px.
- [low] section 1 q-card and meters (look vs W03 deck): The W03 q-card carries the Mennige left rule and the question icon. W04's demo q-card uses an ink rule and no icon, so the question anchor looks different from the deck the owner praised. The distance meter also shows "0.0 t" in Mennige in the correct state, spending the one accent on a non-event. Fix: Use the W03 q-card treatment (Mennige 6 px left rule, question icon from the deck sprite as inline SVG, label "One question, held fixed"). Colour the distance value ink when it is 0.0 t and Mennige only when the distance is above 0.
- [low] section 3 file tree layout and run-record copy: Werk Nord's files sit in three separate groups in two columns (Werk_Nord/Strom left, Werk_Nord/ with the meter CSV lower left, Werk_Nord/Gas right), which breaks the folder's shape. The run record says "The answer on the ledger in the deck and the guide carries this label: Target answer, constructed. Not a recorded run.", which reads like build notes. Fix: Order the groups as they sit on disk: Werk_Nord/Strom, Werk_Nord (meter CSV), Werk_Nord/Gas, then Werk_Sued, Lager_Ost, Flotte, faktoren. Rewrite the run-record line as a label pair: chip "Target answer, constructed. Not a recorded run." plus "Used for the ledger answer in the deck and the guide."

### critique:guide

Verdict: The three pages are close to W03 quality. The build is clean: at 1440, 390 and 320 px there are 0 console errors, 0 warnings, 0 CSP violations and 0 failed requests. Nothing overflows horizontally (the tab strip scrolls on purpose). No U+2013 or U+2014 dashes appear, and the minus signs are U+2212. The field card and the transfer sheet each print on one A4 page, and the guide prints on 16. I checked every number against w04-data.json: all of them are in the JSON, apart from allowlisted non-data facts (years, section numbers, 450/2027/825, 76.9%). The constructed and target labels are present wherever the raw or target answers appear. The problems are: (a) the kit link is broken on all three pages, and the build script explicitly exempts it from its missing-file check; (b) the pages use a different tab order from the demo strip and the registry, and the back links 404 because the registry is not wired in yet; (c) several layout defects on the guide (§12 label collision, orphan answer tile, month strip that does not add up to 12 files, driver bars on two different scales); (d) small print sizes and too much Mennige on the sheets; (e) the word "illustrative" is missing from the factor label. Screenshots and PDFs are in /tmp/claude-0/-home-user-platform/614e303c-f8f0-55ca-be18-13442a9af90b/scratchpad/impl/w04/guide-critique/ (s-guide-d-NN.png desktop, s-guide-m-NN.png phone, s-*-pr-00.png print media, *.pdf).

- [high] guide.html (strip tab 06 Kit and the §15 'Download the kit (.zip)' button), field-card.html and transfer.html strip; source scripts/workshop04/build-pages.mjs lines 321 and 408: Every kit link points to ./esg-kit.zip, which returns 404. The file that exists is kellbrunn-esg-kit.zip, which is also what the registry and its test use. Line 408 of the build explicitly skips esg-kit.zip in the local-reference check, so the build never reports the problem. Fix: In build-pages.mjs change MATS entry to ["kellbrunn-esg-kit.zip", "Kit"] and the §15 button href in guide.template.html to ./kellbrunn-esg-kit.zip, add the size ("Download the kit (.zip, 90 KB)"), delete the `href !== "esg-kit.zip"` exemption on line 408, and rebuild.
- [high] All three pages, strip links 'Back to workshop' (/en/workshops/esg-berichte-mit-ki) and 'Workshop-Seite auf Deutsch' (/workshops/esg-berichte-mit-ki): Both links return 404 on the dev server; the W03 equivalents return 200. ESG_REPORTING_WORKSHOP is only referenced in its own file and its test, so it is not in the workshop registry. Fix: The integrator should register ESG_REPORTING_WORKSHOP in the workshops index/registry that serves /workshops/[slug] and /en/workshops/[slug], then re-check both URLs for 200.
- [high] Material tabs: guide/field-card/transfer vs demo.html vs src/lib/workshops-esg-reporting.ts MATERIALS: There are three different orders. The learner pages use 01 Deck, 02 Demo, 03 Guide, 04 Field card, 05 Transfer, 06 Kit. The demo uses 01 Deck, 02 Demo, 03 Transfer, 04 Guide, 05 Field card and has no Kit tab. The registry uses deck, demo, kit, transfer, guide, card. The same material gets a different number depending on the page. Fix: Adopt the registry's phase order everywhere: 01 Deck, 02 Interactive demo, 03 Kit, 04 Transfer sheet, 05 Learner guide, 06 Field card. Update MATS in build-pages.mjs, and add the Kit tab to the demo strip in the same order.
- [medium] guide.html §12 'Calculate, ask back, refuse' request list (wf-row / wf-row__n): The label column is the two-digit number column from wf-row. 'Calculate' runs into the quote ('Calculate“Sum Scope 2”', no gap). 'Ask back' wraps onto two lines and pushes its description down, which leaves a visible gap. This is the only visibly broken layout on the page (s-guide-d-12.png). Fix: Use a dedicated class, e.g. .ab-row{display:grid;grid-template-columns:7.5rem minmax(0,1fr);column-gap:16px} .ab-row__job{white-space:nowrap}. Below 600px, stack the job label above the quote as a kicker.
- [medium] guide.html §12 intro and labels: The intro says 'Five requests show the three jobs of a useful assistant', but the rows carry five different labels (Calculate, Ask back, Rewrite, Refuse, Explain). A reader cannot map five labels onto the three jobs in the heading. Fix: Label each row with one of the three jobs: Sum Scope 2 → Calculate; Fill in October → Ask back; Green electricity → Refuse the wording, rewrite; Climate-neutral → Refuse; Explain why → Calculate first (bridge), then draft. Alternatively, change the intro to 'Five requests, five different responses'.
- [medium] guide.html §10 target-answer tiles and §2 raw-answer tiles (.nums): §10 puts 5 tiles in a 4-column grid, so 'Total, market-based' sits alone on a second row, indented, with a stray left hairline (s-guide-d-10.png). In §2 the label 'Scope 2, location-based' wraps onto two lines, so its value sits lower than the other values. On phone the tiles are misaligned in the same way (s-guide-m-02.png). Fix: Use a 3-column grid: row 1 has Scope 1, Scope 2 LB and Scope 2 MB; row 2 has Total LB and Total MB. Remove the border-left on the first tile of each row (.nums>div:nth-child(3n+1){border-left:0;padding-left:0}). Reserve two lines for labels (min-height:2.6em) or align values to the bottom (align-self:end), so every value sits on the same baseline.
- [medium] guide.html §2 raw-folder answer box: The fourth tile, '“Market-based” 1,350.0 t', sits next to 'Scope 1 and 2 1,866.5 t' and reads like a second total, but it is Scope 2 only. §10 shows five figures and §2 shows four, so the wrong answer and the target answer cannot be compared tile by tile. Fix: Give §2 the same five tiles as §10, all from JSON: Scope 1 {wrong_s1}; Scope 2 location-based {wrong_s2lb}; Scope 2 “market-based” {wrong_s2mb}; Total location-based {wrong_total_lb} with {wrong_chg_lb_pct} against 2024; Total “market-based” {wrong_total_mb}.
- [medium] guide.html §3 month strip ('As the folder arrived · 12 files, Werk Nord electricity'): The caption says 12 files, but the strip shows only 11 (ten bills plus the March duplicate badge). The Talbrück bill, the 12th file, is not drawn, so the picture does not add up to the number next to it. Fix: Add a detached 13th cell after D, separated by a gap, hatched and marked 'Talbrück, not ours' (Mennige outline). Alternatively, change the caption to '12 files: 10 bills, the March duplicate and the Talbrück bill (not shown)'.
- [medium] guide.html §3 month-strip cells and legend: 'No bill for the month' (dashed, white) and 'Meter reading, grade B' (dashed, pale grey) look almost the same in the legend and on phone. On desktop the labels 'one bill' and 'none' are drawn straight over the hatching and are hard to read, because .mg-t is overridden to background:transparent. Fix: Draw the meter cell as a solid Schiefer (#4f4640) fill with an on-dark 'meter' label, and keep the gap cell as a dashed outline. Add .mg-c--bill .mg-t{background:var(--wf-papier);padding:0 4px} so labels over hatching get a paper chip. Re-check at 390 px, in print and in forced colors.
- [medium] guide.html §11 driver bars (two groups, 'Each group has its own scale'): The −72.2 t location-based grid-factor bar is drawn the same length as the −744.0 t market-based certificate bar, so the chart suggests both effects are the same size. The only warning is a 13 px caption. Fix: Draw both groups on one scale (max = |drv_mb_cert_t|). The location-based bars become about a tenth as wide, which is the point to teach. Keep the value labels, and keep Mennige on the grid-factor bar.
- [medium] guide.html §9 ('22.0 / t below location-based' breaks across lines), §8 ('2026-01- / 15'), and generally every number + unit: Numbers are separated from their units by ordinary spaces, and date or edition strings break at the hyphen. Line breaks such as '22.0' on one line and 't below' on the next (s-guide-d-09.png) look careless and slow reading. Fix: In build-pages.mjs, render {{n:key}} values with U+00A0 between number and unit (and U+00A0 inside 'Lehrwerte v1.0'). Wrap factor editions, dates and IDs in <span class="nowrap"> (white-space:nowrap).
- [medium] field-card.html 'From the case' column: Mennige is used on 9 values in 7 rows, including whole phrases such as '12 files, 11 months'. The deck rule is one rare accent, and W03 marks a single fact per view. Here the accent becomes a second text colour and carries no meaning. Fix: Set the case values in ink, weight 700, tabular figures. Keep Mennige only for the March mark in the month glyph, or for one headline value (e.g. the −495.5 t misread). Apply the same rule in print.
- [medium] lib/w04-pages.css @media print (field card and transfer sheet): The SPEC asks for at least 9 pt on the field card. The print CSS sets .fc thead th, .fc .scene and .sheet-foot to 8.5 pt, and on the transfer sheet .ex to 8.5 pt, .ex .wf-kicker to 7.5 pt and .sentence .de to 8.5 pt. Both sheets still leave about 20–25% of the A4 page empty (s-field-card-pr-00.png, s-transfer-pr-00.png), and the closing-sentence box has only 2 ruled lines for a sentence of about 40 words. Fix: Raise every print size to at least 9 pt (fc body 10.5 pt, head, scene and footer 9 pt; transfer .ex 9 pt, kicker 8.5 pt, DE line 9 pt). Use the freed space to set .sentence .write{min-height:30mm} (4 lines), and increase the .write line pitch to 8 mm for handwriting. Re-check that each sheet still prints on 1 page.
- [medium] All three pages: chip 'Teaching values, not official factors.', figure captions, field-card and transfer footers ('teaching factors'); source w04-data.json meta.factorLabel_en/de: The brief requires the factors to be labelled as illustrative teaching values, but the word 'illustrative' appears nowhere on the learner pages. The chip also ends with a period that the other chips do not have. Fix: Change meta.factorLabel_en to 'Illustrative teaching values, not official factors' (DE 'Illustrative Lehrwerte, keine amtlichen Faktoren') in build_dataset.py and rebuild all surfaces. Change the sheet footers to 'Fictional case, illustrative teaching factors.' Drop the trailing period in chips.
- [low] field-card.html masthead month glyph (SVG, aria-hidden): The 13-rectangle glyph (raised red March, dashed October, N and D drawn as two squares although they are one bill) has no label or caption. A reader cannot decode it, so it works as decoration that hints at meaning without saying it. Fix: Add a caption next to it, e.g. 'Werk Nord 2025 as it arrived: March twice, October missing, November and December on one bill', and merge N+D into one wide cell. Alternatively, remove the glyph.
- [low] guide.html §9 bridge chart: The reveal asks 'Why is 948.0 t wrong for market-based?', but the chart never shows 948.0 t. The step between the certificate bar and the residual-mix bar has no value, so the reader cannot see where the wrong number comes from. Fix: Draw a dashed tick at {mb_grid_avg_wrong_s2_t} (948.0 t) at the step, labelled '948.0 t if the rest stayed at the grid average (wrong)', in Schiefer caption style, at least 13 px.
- [low] guide.html kickers: 'Before the route' (§1) is deck-internal jargon that means nothing to a reader of the guide. 'Honest limits' sits over §12 'Calculate, ask back, refuse', which is about what a good assistant does, not a limit. Fix: §1 kicker: 'The case'. §12 kicker: 'Ask again' (same as §10–11) or 'What a good assistant does'. Keep 'Honest limits' only on §13.
- [low] guide.html §2 reveal prompt; §16 recall answer 3; field-card row 7: Three wording problems. (1) 'what does it actually test?' uses the filler 'actually' (the same fix as V23). (2) The §16 answer says certificates are 'usually not your doing', but buying guarantees of origin is the company's own decision. This contradicts §11's point about separating your own use from certificates. (3) Field card row 7 reads '72.2 grid, 30.1 own use' with no units, and 'grid' is ambiguous. Fix: (1) 'Which of the five checks would you have trusted most, and what does it test?' (2) 'The grid factor. Certificates are your purchase, but they lower the market-based figure without any change in your use. Here 71% of the location-based decrease came from the grid factor.' (3) '−102.3 t: 72.2 t grid factor, 30.1 t own use'.
- [low] transfer.html on phone (390 px): The dashed writing boxes cannot be typed into, so on a phone they are about 100 px of dead space per box. Five boxes plus the sentence box add roughly 600 px of empty scrolling. Fix: Below 600 px on screen, collapse .write to a single dashed line with the note 'Print this sheet to write on it', or hide it (keep it in @media print). Alternatively, use a plain <textarea> without storage.
- [low] guide.html §2 paragraph after the raw-answer box: The constructed label appears four times within one screen: the chip, the box kicker, the box footnote, and again in the body ('This answer is constructed from documented failure modes. It shows what happens when all six traps fire; it is not a recorded run.'). The repetition reads as defensive and adds nothing. Fix: Keep the chip and the box kicker plus footnote (the required labels). Change the body sentence to 'The box above shows what these six errors do together.' or drop it.
